{
  "name": "@ammo-exchange/contracts",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./abis": "./src/abis/index.ts"
  },
  "scripts": {
    "build": "command -v forge >/dev/null && forge build && tsx scripts/export-abis.ts || echo 'Skipping forge build (not installed), using committed ABIs'",
    "test": "forge test",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
