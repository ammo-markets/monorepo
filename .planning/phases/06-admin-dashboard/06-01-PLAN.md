---
phase: 06-admin-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/hooks/use-keeper-check.ts
  - apps/web/app/admin/layout.tsx
  - apps/web/app/admin/page.tsx
  - apps/web/app/admin/mint-orders/page.tsx
  - apps/web/app/admin/redeem-orders/page.tsx
  - apps/web/features/admin/index.ts
  - apps/web/features/admin/admin-layout-gate.tsx
  - apps/web/features/admin/admin-sidebar.tsx
  - apps/web/features/admin/mint-orders-table.tsx
  - apps/web/features/admin/redeem-orders-table.tsx
  - apps/web/app/api/admin/orders/route.ts
autonomous: true

must_haves:
  truths:
    - "Non-keeper wallets see an Access Denied message on /admin/* routes, not a broken page"
    - "Disconnected wallets see a Connect Wallet prompt on /admin/* routes"
    - "Admin sees a list of pending mint orders with wallet address, USDC amount, caliber, and timestamp"
    - "Admin sees a list of pending redeem orders with wallet address, token amount, caliber, shipping address, and KYC status"
  artifacts:
    - path: "apps/web/hooks/use-keeper-check.ts"
      provides: "Client-side isKeeper on-chain check via useReadContract"
      exports: ["useKeeperCheck"]
    - path: "apps/web/features/admin/admin-layout-gate.tsx"
      provides: "Three-state gate: loading, not-connected, not-keeper, keeper-children"
      exports: ["AdminLayoutGate"]
    - path: "apps/web/app/admin/layout.tsx"
      provides: "Admin layout wrapping all /admin/* routes with keeper gate and sidebar"
    - path: "apps/web/app/api/admin/orders/route.ts"
      provides: "GET endpoint returning pending orders with type filter"
      exports: ["GET"]
    - path: "apps/web/features/admin/mint-orders-table.tsx"
      provides: "Table rendering pending mint orders"
      exports: ["MintOrdersTable"]
    - path: "apps/web/features/admin/redeem-orders-table.tsx"
      provides: "Table rendering pending redeem orders with shipping/KYC info"
      exports: ["RedeemOrdersTable"]
  key_links:
    - from: "apps/web/app/admin/layout.tsx"
      to: "apps/web/features/admin/admin-layout-gate.tsx"
      via: "AdminLayoutGate wraps children"
      pattern: "AdminLayoutGate"
    - from: "apps/web/features/admin/admin-layout-gate.tsx"
      to: "apps/web/hooks/use-keeper-check.ts"
      via: "useKeeperCheck hook for on-chain role check"
      pattern: "useKeeperCheck"
    - from: "apps/web/features/admin/mint-orders-table.tsx"
      to: "apps/web/app/api/admin/orders/route.ts"
      via: "fetch /api/admin/orders?type=MINT"
      pattern: "api/admin/orders"
    - from: "apps/web/features/admin/redeem-orders-table.tsx"
      to: "apps/web/app/api/admin/orders/route.ts"
      via: "fetch /api/admin/orders?type=REDEEM"
      pattern: "api/admin/orders"
---

<objective>
Build the admin dashboard shell: keeper-gated layout, sidebar navigation, admin API route for pending orders, and order queue pages for both mint and redeem orders.

Purpose: Keepers need to see pending orders before they can act on them. This plan establishes the admin area structure and read-only order views that plan 06-02 will enhance with finalization actions and protocol stats.

Output: A working /admin section with keeper access control, pending mint orders page, and pending redeem orders page -- all reading real data from the database.
</objective>

<execution_context>
@/Users/chiranjibipoudyal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiranjibipoudyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-dashboard/06-RESEARCH.md

# Existing patterns to follow:

@apps/web/hooks/use-mint-transaction.ts -- Explicit return type pattern (TS2742 prevention)
@apps/web/hooks/use-token-balances.ts -- useReadContract pattern
@apps/web/app/api/orders/route.ts -- Prisma query + PRISMA_TO_CALIBER + serializeBigInts
@apps/web/lib/errors.ts -- parseContractError (already has NotKeeper)
@apps/web/lib/serialize.ts -- serializeBigInts helper
@apps/web/app/layout.tsx -- Root layout structure
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin hooks, layout gate, API route, and sidebar</name>
  <files>
    apps/web/hooks/use-keeper-check.ts
    apps/web/features/admin/index.ts
    apps/web/features/admin/admin-layout-gate.tsx
    apps/web/features/admin/admin-sidebar.tsx
    apps/web/app/admin/layout.tsx
    apps/web/app/admin/page.tsx
    apps/web/app/api/admin/orders/route.ts
  </files>
  <action>
    **1. Create `hooks/use-keeper-check.ts`:**
    - "use client" directive
    - Import useAccount and useReadContract from wagmi
    - Import AmmoManagerAbi from @ammo-exchange/contracts/abis
    - Import CONTRACT_ADDRESSES from @ammo-exchange/shared
    - Call `useReadContract` with `functionName: "isKeeper"`, `args: [address!]`, `query: { enabled: isConnected && !!address }`
    - Address is `CONTRACT_ADDRESSES.fuji.manager`
    - MUST provide explicit return type: `{ isKeeper: boolean; isLoading: boolean; isConnected: boolean; address: \`0x${string}\` | undefined }`
    - Return `{ isKeeper: !!data, isLoading, isConnected, address }` from useAccount

    **2. Create `features/admin/admin-layout-gate.tsx`:**
    - "use client" directive
    - Import useKeeperCheck from @/hooks/use-keeper-check
    - Accept `{ children: ReactNode }` prop
    - Three render states:
      - `isLoading` → show a centered spinner/skeleton with "Checking access..." text
      - `!isConnected` → show a centered card with Shield icon, "Connect Wallet" heading, message "Connect a keeper wallet to access the admin dashboard", and optionally import WalletButton from features/layout for the connect action
      - `!isKeeper` → show a centered card with AlertTriangle icon (lucide-react), "Access Denied" heading, message "Your wallet is not authorized as a keeper. Only keeper wallets can access the admin dashboard."
      - `isKeeper` → render `{children}`
    - Style: centered cards using Tailwind, consistent with app's dark theme (bg-zinc-900, text-zinc-100, border-zinc-800)

    **3. Create `features/admin/admin-sidebar.tsx`:**
    - "use client" directive (uses usePathname)
    - Import Link from next/link, usePathname from next/navigation
    - Three nav links: Dashboard (/admin), Mint Orders (/admin/mint-orders), Redeem Orders (/admin/redeem-orders)
    - Icons from lucide-react: LayoutDashboard, ArrowUpCircle, ArrowDownCircle
    - Active link highlighted with amber accent (text-amber-400, bg-amber-400/10)
    - Fixed-width sidebar (w-64) with dark background (bg-zinc-950, border-r border-zinc-800)
    - Show "Admin" heading at top with Shield icon

    **4. Create `app/admin/layout.tsx`:**
    - Server component that imports AdminLayoutGate and AdminSidebar
    - Wraps children in AdminLayoutGate
    - Inside the gate: flex layout with AdminSidebar on the left and children in a scrollable main area
    - Main area has padding (p-6 or p-8) and max-width for readability

    **5. Create `app/admin/page.tsx`:**
    - Placeholder page showing "Admin Dashboard" heading with a brief message "Protocol stats coming soon."
    - This will be replaced by the stats dashboard in plan 06-02

    **6. Create `app/api/admin/orders/route.ts`:**
    - Import prisma from @ammo-exchange/db
    - Import PRISMA_TO_CALIBER from @ammo-exchange/shared, and type Caliber
    - Import serializeBigInts from @/lib/serialize
    - Accept optional `type` query param: "MINT" | "REDEEM"
    - Query: `prisma.order.findMany({ where: { status: "PENDING", ...(type ? { type } : {}) }, orderBy: { createdAt: "desc" }, include: { shippingAddress: true, user: { select: { kycStatus: true } } } })`
    - Map results: convert caliber via PRISMA_TO_CALIBER, convert dates to ISO strings
    - Return `Response.json({ orders: serializeBigInts(mapped) })`
    - No auth check on API route (testnet -- UI gate is sufficient, contract enforces real security)

    **7. Create `features/admin/index.ts`:**
    - Barrel export for AdminLayoutGate, AdminSidebar

  </action>
  <verify>
    Run `pnpm --filter @ammo-exchange/web check` to confirm no TypeScript errors. Verify files exist at all listed paths.
  </verify>
  <done>
    useKeeperCheck hook reads isKeeper from AmmoManager contract. AdminLayoutGate blocks non-keepers with a clear message. Admin layout renders sidebar + gated content. API route returns pending orders from database.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mint orders and redeem orders queue pages</name>
  <files>
    apps/web/features/admin/mint-orders-table.tsx
    apps/web/features/admin/redeem-orders-table.tsx
    apps/web/app/admin/mint-orders/page.tsx
    apps/web/app/admin/redeem-orders/page.tsx
  </files>
  <action>
    **1. Create `features/admin/mint-orders-table.tsx`:**
    - "use client" directive
    - Fetch pending mint orders from `/api/admin/orders?type=MINT` using useEffect + useState (or a simple fetch wrapper; do NOT add TanStack Query if not already imported in admin features -- check existing usage first, if TanStack Query is already available in the app, use it)
    - Define AdminMintOrder type inline: `{ id: string; walletAddress: string | null; caliber: string; amount: string; createdAt: string; onChainOrderId: string | null; txHash: string | null }`
    - Render a table with columns: Order ID (first 8 chars), Wallet (truncated with `truncateAddress` from @/lib/utils), Caliber, USDC Amount (format: divide amount string by 1e6 for USDC display), Time (format with `formatDistanceToNow` from date-fns), Actions (placeholder button "Finalize" -- disabled for now, wired in plan 06-02)
    - Empty state: centered message "No pending mint orders" with a muted icon
    - Loading state: simple "Loading..." or skeleton rows
    - Error state: "Failed to load orders" with retry button
    - Style: standard table with zebra-striped rows or subtle borders, dark theme

    **2. Create `features/admin/redeem-orders-table.tsx`:**
    - "use client" directive
    - Same fetch pattern as mint orders but `?type=REDEEM`
    - Define AdminRedeemOrder type inline with additional fields: `user: { kycStatus: string } | null; shippingAddress: { ... } | null`
    - Table columns: Order ID (first 8 chars), Wallet (truncated), Caliber, Token Amount (divide amount string by 1e18, floor to whole rounds), KYC Status (badge: green for APPROVED, yellow for PENDING, red for REJECTED, gray for NONE), Shipping (show city/state if address exists, "None" otherwise), Time, Actions (placeholder "Finalize" button -- disabled, wired in 06-02)
    - KYC badge: use colored Badge component or simple styled span
    - Same empty/loading/error states as mint table

    **3. Create `app/admin/mint-orders/page.tsx`:**
    - Import MintOrdersTable from @/features/admin/mint-orders-table
    - Render page heading "Pending Mint Orders" with a brief description
    - Render MintOrdersTable component

    **4. Create `app/admin/redeem-orders/page.tsx`:**
    - Import RedeemOrdersTable from @/features/admin/redeem-orders-table
    - Render page heading "Pending Redeem Orders" with a brief description
    - Render RedeemOrdersTable component

    **Update `features/admin/index.ts`:**
    - Add exports for MintOrdersTable and RedeemOrdersTable

    **Important details:**
    - For mint orders, the `amount` field from the API is the USDC amount in 6 decimals (raw bigint serialized as string). Display as `(Number(amount) / 1e6).toFixed(2)` with "USDC" suffix.
    - For redeem orders, the `amount` field is the ammo token amount in 18 decimals. Display as `Math.floor(Number(amount) / 1e18)` with "rounds" suffix.
    - The "Finalize" buttons should be present but disabled (`disabled` attribute) with a tooltip or title "Coming soon". Plan 06-02 will wire them to the finalize hooks.
    - Use `truncateAddress` from @/lib/utils (already exists in the codebase).
    - Use `formatDistanceToNow` from date-fns for relative timestamps (already installed).

  </action>
  <verify>
    Run `pnpm --filter @ammo-exchange/web check` to confirm no TypeScript errors. Run `pnpm --filter @ammo-exchange/web build` to verify the pages compile and render without errors.
  </verify>
  <done>
    Admin can navigate to /admin/mint-orders and see all pending mint orders from the database in a table with wallet, amount, caliber, and timestamp. Admin can navigate to /admin/redeem-orders and see all pending redeem orders with KYC status and shipping info. Finalize buttons are present but disabled (wired in plan 06-02).
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @ammo-exchange/web check` passes with no TypeScript errors
2. `pnpm --filter @ammo-exchange/web build` succeeds
3. Files exist: hooks/use-keeper-check.ts, app/admin/layout.tsx, app/admin/page.tsx, app/admin/mint-orders/page.tsx, app/admin/redeem-orders/page.tsx, features/admin/admin-layout-gate.tsx, features/admin/admin-sidebar.tsx, features/admin/mint-orders-table.tsx, features/admin/redeem-orders-table.tsx, app/api/admin/orders/route.ts
4. The admin layout gate correctly imports and uses useKeeperCheck
5. The API route queries Prisma for PENDING orders with PRISMA_TO_CALIBER mapping
</verification>

<success_criteria>

- useKeeperCheck hook calls AmmoManager.isKeeper on-chain
- AdminLayoutGate shows three states: loading, connect wallet, access denied, or renders children
- Admin sidebar provides navigation between dashboard, mint orders, and redeem orders
- /api/admin/orders returns pending orders from database with caliber mapping and BigInt serialization
- Mint orders table displays wallet, USDC amount, caliber, timestamp
- Redeem orders table displays wallet, token amount, caliber, KYC status, shipping info, timestamp
- All files pass TypeScript strict mode check
  </success_criteria>

<output>
After completion, create `.planning/phases/06-admin-dashboard/06-01-SUMMARY.md`
</output>
