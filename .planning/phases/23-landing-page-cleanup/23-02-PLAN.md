---
phase: 23-landing-page-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/features/trade/swap-widget/index.tsx
  - apps/web/features/trade/swap-widget/swap-types.ts
  - apps/web/features/trade/swap-widget/token-icons.tsx
  - apps/web/features/trade/swap-widget/token-selector.tsx
  - apps/web/features/trade/swap-widget/swap-tab.tsx
  - apps/web/features/trade/swap-widget/lend-borrow-tab.tsx
  - apps/web/features/trade/swap-widget/swap-widget-content.tsx
autonomous: true

must_haves:
  truths:
    - "Every sub-component file in swap-widget/ is under 300 lines"
    - "SwapWidget is the only export from the swap-widget/ folder"
    - "The swap widget functions identically before and after refactor (no behavior changes)"
    - "The old swap-widget.tsx file is deleted"
  artifacts:
    - path: "apps/web/features/trade/swap-widget/index.tsx"
      provides: "SwapWidget re-export from folder"
    - path: "apps/web/features/trade/swap-widget/swap-types.ts"
      provides: "TokenId, Token types, buildTokens, getToken helpers"
    - path: "apps/web/features/trade/swap-widget/token-icons.tsx"
      provides: "UsdcIcon, UniswapLogo, AaveLogo, TokenIcon components"
    - path: "apps/web/features/trade/swap-widget/token-selector.tsx"
      provides: "TokenSelector dropdown component"
    - path: "apps/web/features/trade/swap-widget/swap-tab.tsx"
      provides: "SwapTab form component"
    - path: "apps/web/features/trade/swap-widget/lend-borrow-tab.tsx"
      provides: "LendBorrowTab cards component"
    - path: "apps/web/features/trade/swap-widget/swap-widget-content.tsx"
      provides: "SwapWidgetContent header + tab switching"
  key_links:
    - from: "apps/web/features/trade/swap-widget/index.tsx"
      to: "apps/web/features/trade/swap-widget/swap-widget-content.tsx"
      via: "import and re-export"
      pattern: "SwapWidgetContent"
    - from: "apps/web/features/trade/swap-widget/swap-widget-content.tsx"
      to: "apps/web/features/trade/swap-widget/swap-tab.tsx"
      via: "renders SwapTab"
      pattern: "SwapTab"
    - from: "apps/web/features/trade/swap-widget/swap-tab.tsx"
      to: "apps/web/features/trade/swap-widget/token-selector.tsx"
      via: "renders TokenSelector"
      pattern: "TokenSelector"
---

<objective>
Refactor the 749-line swap-widget.tsx monolith into a co-located folder of sub-components, each under 300 lines.

Purpose: The swap widget is the largest component in the codebase. Splitting it improves maintainability and readability without changing any behavior.

Output: A `swap-widget/` folder with 7 files, all under 300 lines, re-exporting only `SwapWidget` from `index.tsx`. The original `swap-widget.tsx` is deleted.
</objective>

<execution_context>
@/Users/chiranjibipoudyal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiranjibipoudyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/web/features/trade/swap-widget.tsx (monolith to refactor - 749 lines)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create swap-widget/ folder with sub-component files extracted from swap-widget.tsx</name>
  <files>
    apps/web/features/trade/swap-widget/swap-types.ts
    apps/web/features/trade/swap-widget/token-icons.tsx
    apps/web/features/trade/swap-widget/token-selector.tsx
    apps/web/features/trade/swap-widget/swap-tab.tsx
    apps/web/features/trade/swap-widget/lend-borrow-tab.tsx
    apps/web/features/trade/swap-widget/swap-widget-content.tsx
    apps/web/features/trade/swap-widget/index.tsx
  </files>
  <action>
    Extract the monolithic `swap-widget.tsx` (749 lines) into 7 files in a `swap-widget/` folder. All sub-components are internal -- only `SwapWidget` is exported from `index.tsx`.

    **File breakdown (reference original line numbers):**

    1. **`swap-types.ts`** (~50 lines) - Extract from lines 1-53:
       - `TokenId` type alias (`Caliber | "USDC"`)
       - `Token` interface
       - `CALIBERS` constant array
       - `buildTokens(marketData)` function
       - `getToken(tokens, id)` function
       - Import `Caliber` from shared, `CALIBER_SPECS` from shared, `MarketCaliberFromAPI` from `@/lib/types`

    2. **`token-icons.tsx`** (~100 lines) - Extract from lines 57-157:
       - `UsdcIcon` component
       - `UniswapLogo` component
       - `AaveLogo` component
       - `TokenIcon` component (uses caliberIcons from `@/features/shared/caliber-icons`)
       - Import `TokenId` from `./swap-types`

    3. **`token-selector.tsx`** (~90 lines) - Extract from lines 161-249:
       - `TokenSelector` component with dropdown state, outside-click handler, token list
       - Import `TokenId`, `Token`, `getToken` from `./swap-types`
       - Import `TokenIcon` from `./token-icons`
       - Import `ChevronDown` from lucide

    4. **`swap-tab.tsx`** (~260 lines) - Extract from lines 253-513:
       - `SwapTab` component with pay/receive inputs, direction swap, exchange details, CTA
       - Import `TokenId`, `Token`, `getToken` from `./swap-types`
       - Import `TokenSelector` from `./token-selector`
       - Import `TokenIcon` from `./token-icons` (for the exchange rate display if needed)
       - Import `UniswapLogo` from `./token-icons` (for "Powered by" footer)
       - Import `useAuth` from `@/contexts/auth-context`
       - Import icons from lucide: `ChevronDown`, `ChevronUp`, `ExternalLink`, `ArrowDownUp`

    5. **`lend-borrow-tab.tsx`** (~70 lines) - Extract from lines 517-586:
       - `LendBorrowTab` component with Aave supply/borrow cards
       - Import `AaveLogo` from `./token-icons`
       - Import `ExternalLink` from lucide

    6. **`swap-widget-content.tsx`** (~70 lines) - Extract from lines 590-659:
       - `SwapWidgetContent` component with header, tab switching, tab content rendering
       - Import `useMarketData` from `@/hooks/use-market-data`
       - Import `buildTokens` from `./swap-types`
       - Import `SwapTab` from `./swap-tab`
       - Import `LendBorrowTab` from `./lend-borrow-tab`
       - Import `X` from lucide

    7. **`index.tsx`** (~90 lines) - Extract from lines 663-749:
       - `SwapWidgetProps` interface
       - `SwapWidget` component (the exported modal/drawer wrapper)
       - Re-export: `export { SwapWidget }` (this is the ONLY public export)
       - Import `SwapWidgetContent` from `./swap-widget-content`
       - Import `Drawer` from `vaul`
       - Import `ArrowDownUp` from lucide
       - Add `"use client"` directive

    **Important implementation notes:**
    - Add `"use client"` to all `.tsx` files that use React hooks or browser APIs (token-selector, swap-tab, swap-widget-content, index.tsx). The `.ts` types file does NOT need it. token-icons.tsx does NOT need it (pure render). lend-borrow-tab.tsx does NOT need it (pure render).
    - All imports between sub-components use relative paths (`./swap-types`, `./token-icons`, etc.)
    - Keep the exact same code -- this is a pure structural refactor. No behavior changes, no style changes, no logic changes.
    - After creating all files, **delete the original** `apps/web/features/trade/swap-widget.tsx`.

    **Verify no external consumers break:**
    - Search for imports of `swap-widget` in the codebase. The import path `@/features/trade/swap-widget` will automatically resolve to the new `swap-widget/index.tsx` thanks to Node module resolution. No external import changes needed.
  </action>
  <verify>
    1. Run `pnpm --filter @ammo-exchange/web check` to confirm no type errors.
    2. Verify each file is under 300 lines: `wc -l apps/web/features/trade/swap-widget/*.{ts,tsx}`
    3. Verify the old file is deleted: `ls apps/web/features/trade/swap-widget.tsx` should fail.
    4. Verify the import still resolves: `grep -r "swap-widget" apps/web/ --include="*.tsx" --include="*.ts"` shows only internal imports within the folder + external consumers importing from the folder.
    5. Manually test in browser: open the swap widget, test the token selector dropdown, switch between Swap and Lend/Borrow tabs, and verify the mobile drawer opens/closes correctly.
  </verify>
  <done>
    swap-widget.tsx (749 lines) is split into 7 files in swap-widget/ folder, all under 300 lines. SwapWidget is the only public export. Original file deleted. All type checks pass. No behavior changes.
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @ammo-exchange/web check` passes with no type errors
2. No file in `apps/web/features/trade/swap-widget/` exceeds 300 lines
3. Only `SwapWidget` is exported from `swap-widget/index.tsx`
4. Original `swap-widget.tsx` file no longer exists
5. The swap widget UI functions identically (opens on trigger, tabs work, token selector works, mobile drawer works)
</verification>

<success_criteria>
- Swap widget is refactored into sub-components each under 300 lines
- Only SwapWidget is the public export from the folder
- No behavior changes -- pure structural refactor
- Original monolithic file is deleted
</success_criteria>

<output>
After completion, create `.planning/phases/23-landing-page-cleanup/23-02-SUMMARY.md`
</output>
