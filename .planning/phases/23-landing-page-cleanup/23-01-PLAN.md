---
phase: 23-landing-page-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/features/home/hero.tsx
  - apps/web/features/home/protocol-stats.tsx
  - apps/web/app/api/stats/route.ts
  - apps/web/hooks/use-protocol-stats.ts
autonomous: true

must_haves:
  truths:
    - "Trust strip text is white (#FFFFFF or similar high-contrast) against the dark background, clearly readable"
    - "Trust strip has a subtle separator distinguishing it from the rest of the hero section"
    - "Trust strip combines protocol credibility messages AND social proof numbers in one strip"
    - "Landing page displays three social proof stats: total trading volume (USDC), registered users, and rounds tokenized"
    - "Stats show abbreviated large number formatting ($1.2M, 500+, 10K+)"
    - "Stats animate from 0 to their value when scrolling into view (count-up animation)"
    - "Stats are live data fetched from API on each page load"
  artifacts:
    - path: "apps/web/features/home/hero.tsx"
      provides: "Trust strip with white text, subtle separator, and credibility + traction messaging"
    - path: "apps/web/features/home/protocol-stats.tsx"
      provides: "Three social proof stats with count-up animation and abbreviated formatting"
    - path: "apps/web/app/api/stats/route.ts"
      provides: "API endpoint returning totalVolumeUsdc, registeredUsers, roundsTokenized"
    - path: "apps/web/hooks/use-protocol-stats.ts"
      provides: "Hook consuming updated stats API response shape"
  key_links:
    - from: "apps/web/features/home/protocol-stats.tsx"
      to: "apps/web/hooks/use-protocol-stats.ts"
      via: "useProtocolStats hook call"
      pattern: "useProtocolStats"
    - from: "apps/web/hooks/use-protocol-stats.ts"
      to: "/api/stats"
      via: "fetch call"
      pattern: "fetch.*api/stats"
    - from: "apps/web/app/api/stats/route.ts"
      to: "prisma"
      via: "database queries for volume, users, and supply"
      pattern: "prisma\\.(order|user|protocolStats)"
---

<objective>
Fix trust strip contrast and add live social proof stats to the landing page.

Purpose: The trust strip text is currently using `var(--text-muted)` which lacks contrast on the dark background. Users need to see high-contrast credibility messaging alongside real protocol traction numbers (volume, users, rounds) to build trust before entering the app.

Output: High-contrast white trust strip with credibility + traction messaging, and three live social proof stats with count-up animation in the ProtocolStats section.
</objective>

<execution_context>
@/Users/chiranjibipoudyal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiranjibipoudyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/web/features/home/hero.tsx (trust strip to fix)
@apps/web/features/home/protocol-stats.tsx (stats section to update)
@apps/web/app/api/stats/route.ts (stats API to extend)
@apps/web/hooks/use-protocol-stats.ts (hook to update type)
@packages/db/prisma/schema.prisma (ProtocolStats model, Order model, User model)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix trust strip contrast and update API to return social proof data</name>
  <files>
    apps/web/features/home/hero.tsx
    apps/web/app/api/stats/route.ts
    apps/web/hooks/use-protocol-stats.ts
  </files>
  <action>
    1. **Fix trust strip in hero.tsx** (per locked decisions: white text, subtle separator, text only, no icons):
       - Change the trust strip div's `style={{ color: "var(--text-muted)" }}` to `style={{ color: "#FFFFFF" }}` for maximum readability.
       - Add a subtle separator directly on the trust strip div (the `<div>` at ~line 118 with className `mt-16 flex flex-wrap...`). Add `borderTop: "1px solid rgba(255,255,255,0.1)"` and `paddingTop: "1.5rem"` to its existing `style` prop. This places the border directly on the trust strip element, visually separating it from the CTAs above. No wrapper div needed.
       - Combine credibility AND traction messaging: Keep the existing credibility items ("Backed by physical inventory", "Insured storage", "USDC payments", "Avalanche blockchain") but they're already good. The traction numbers will live in the ProtocolStats section below -- the trust strip stays text-only per decision.
       - The separator dividers (`|` characters) between items should also be white or near-white for visibility. Change their style from `color: "var(--border-hover)"` to `color: "rgba(255,255,255,0.3)"`.

    2. **Extend stats API** (`app/api/stats/route.ts`):
       - Keep the existing per-caliber `stats` array in the response (other consumers may use it).
       - Add three new top-level fields to the response:
         ```
         totalVolumeUsdc: number    // Sum of all COMPLETED order USDC amounts
         registeredUsers: number    // Count of all users in the User table
         roundsTokenized: number    // Sum of totalMinted across all calibers from ProtocolStats
         ```
       - For `totalVolumeUsdc`: Query `prisma.order.aggregate({ where: { status: "COMPLETED" }, _sum: { usdcAmount: true } })`. The `usdcAmount` field stores the USDC value. Convert the BigInt sum to a number (it's USDC with 6 decimals, so divide by 1_000_000 to get dollar value). If null, return 0.
       - For `registeredUsers`: Query `prisma.user.count()`.
       - For `roundsTokenized`: Already have `rows` from `prisma.protocolStats.findMany()`. Sum all `totalMinted` values (they're strings, parse with `Number()`).
       - Return all three alongside the existing `stats` array.

    3. **Update hook type** (`hooks/use-protocol-stats.ts`):
       - Add `totalVolumeUsdc: number`, `registeredUsers: number`, `roundsTokenized: number` to the `ProtocolStatsResponse` interface.
       - These will now be available for the ProtocolStats component.
  </action>
  <verify>
    Run `pnpm --filter @ammo-exchange/web check` to confirm no type errors. Visually inspect: trust strip text should be white and clearly visible.
  </verify>
  <done>
    Trust strip text is white (#FFFFFF) with subtle separator. Stats API returns totalVolumeUsdc, registeredUsers, and roundsTokenized alongside existing per-caliber stats. Hook type matches new response shape.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ProtocolStats component to display three social proof stats with count-up animation</name>
  <files>
    apps/web/features/home/protocol-stats.tsx
  </files>
  <action>
    1. **Simplify to three stats** (per locked decision: total trading volume, registered users, rounds tokenized):
       - Replace the existing 4-stat grid (TVL, Rounds Tokenized, Unique Holders, Total Volume) with exactly 3 stats. Specifically, **remove the "Unique Holders" stat** (which uses the old `uniqueHolders` field) and **replace it with "Registered Users"** using the new `registeredUsers` field from the updated API response.
         - **Total Trading Volume**: Use `protocolStats.totalVolumeUsdc` with abbreviated formatting ($1.2M, $500K, etc.). Use the existing `formatCompact` function.
         - **Registered Users**: Use `protocolStats.registeredUsers` (NOT `uniqueHolders`). Format as `500+` style (append `+` if > 0). For large numbers use compact: `1.2K+`, `10K+`.
         - **Rounds Tokenized**: Use `protocolStats.roundsTokenized`. Format as `10K+`, `1.2M+` style for large numbers, or exact for small.
       - Labels: "Trading Volume", "Registered Users", "Rounds Tokenized"
       - Also remove the TVL stat (it is not one of the three locked stats).

    2. **Update grid layout**: Change from `grid-cols-2 md:grid-cols-4` to `grid-cols-1 sm:grid-cols-3` for 3 items.

    3. **Simplified data flow**: Remove the `useMarketData()` dependency since all three stats now come from the single `useProtocolStats()` hook via the API. Remove the `useMemo` that computed stats from market data. The component becomes much simpler:
       - Call `useProtocolStats()`
       - If data not loaded, show placeholder dashes
       - Otherwise format and display the three values

    4. **Keep the existing `useCountUp` hook and `StatItem` component** -- they already implement the scroll-into-view count-up animation per the locked decision. No changes needed to the animation logic.

    5. **Add abbreviated formatting helper** for users and rounds (the existing `formatCompact` handles dollar amounts). Create a `formatCount` helper:
       ```
       function formatCount(n: number): string {
         if (n >= 1_000_000) return `${(n / 1_000_000).toFixed(1)}M+`;
         if (n >= 1_000) return `${(n / 1_000).toFixed(1)}K+`;
         if (n > 0) return `${n}+`;
         return "0";
       }
       ```
  </action>
  <verify>
    Run `pnpm --filter @ammo-exchange/web check` to confirm no type errors. The ProtocolStats section should display 3 stats with count-up animation when scrolled into view.
  </verify>
  <done>
    ProtocolStats shows exactly 3 stats (trading volume, registered users, rounds tokenized) with abbreviated formatting and count-up animation on scroll. Data comes from the live API endpoint.
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @ammo-exchange/web check` passes with no type errors
2. Trust strip text on landing page hero is white and clearly readable against dark background
3. Trust strip has a visible separator distinguishing it from the CTA section above
4. ProtocolStats section shows exactly 3 stats: Trading Volume, Registered Users, Rounds Tokenized
5. Stats use abbreviated formatting ($1.2M, 500+, 10K+)
6. Stats animate from 0 to value when scrolling into view
7. Stats are fetched from /api/stats endpoint with live database data
</verification>

<success_criteria>
- Trust strip text has adequate contrast (white on dark)
- Landing page displays 3 real social proof stats from live API data
- Stats use abbreviated large number formatting
- Count-up animation triggers when stats scroll into view
</success_criteria>

<output>
After completion, create `.planning/phases/23-landing-page-cleanup/23-01-SUMMARY.md`
</output>
