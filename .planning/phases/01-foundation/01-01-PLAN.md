---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/MockUSDC.sol
  - packages/contracts/src/MockPriceOracle.sol
  - packages/contracts/script/DeployFuji.s.sol
  - packages/contracts/Makefile
  - packages/contracts/foundry.toml
autonomous: false
user_setup:
  - service: Avalanche Fuji Testnet
    why: "Contract deployment and verification"
    env_vars:
      - name: PRIVATE_KEY
        source: "Your testnet wallet private key (must have Fuji AVAX for gas)"
      - name: SNOWTRACE_API_KEY
        source: "Register at routescan.io for free API key, or try placeholder 'verifyContract'"
      - name: FUJI_RPC_URL
        source: "Already in .env or use default https://api.avax-test.network/ext/bc/C/rpc"

must_haves:
  truths:
    - "MockUSDC contract has exactly 6 decimals and a public faucet capped at 10,000 USDC per call"
    - "DeployFuji.s.sol deploys all contracts in sequence: MockUSDC -> MockPriceOracles -> AmmoManager -> AmmoFactory -> 4 calibers"
    - "All testnet roles (keeper, treasury, guardian, feeRecipient) are set to the deployer wallet in the same deploy script"
    - "Makefile has fuji_check (dry run) and fuji_deploy (broadcast + verify) targets"
    - "foundry.toml includes evm_version = cancun and [etherscan] section for Fuji verification"
    - "forge script simulation (fuji_check) runs without revert"
  artifacts:
    - path: "packages/contracts/src/MockUSDC.sol"
      provides: "ERC20 with public faucet for testnet"
      contains: "FAUCET_CAP"
    - path: "packages/contracts/src/MockPriceOracle.sol"
      provides: "Deployable price oracle for testnet calibers"
      contains: "IPriceOracle"
    - path: "packages/contracts/script/DeployFuji.s.sol"
      provides: "Single deployment script for all contracts + role setup"
      contains: "vm.startBroadcast"
    - path: "packages/contracts/Makefile"
      provides: "Make targets for Fuji deployment"
      contains: "fuji_deploy"
    - path: "packages/contracts/foundry.toml"
      provides: "Updated Foundry config with EVM version and verification endpoints"
      contains: "evm_version"
  key_links:
    - from: "packages/contracts/script/DeployFuji.s.sol"
      to: "packages/contracts/src/MockUSDC.sol"
      via: "import and deployment"
      pattern: "import.*MockUSDC"
    - from: "packages/contracts/script/DeployFuji.s.sol"
      to: "packages/contracts/src/MockPriceOracle.sol"
      via: "import and deployment (one oracle per caliber)"
      pattern: "import.*MockPriceOracle"
    - from: "packages/contracts/script/DeployFuji.s.sol"
      to: "packages/contracts/src/AmmoFactory.sol"
      via: "import and factory.createCaliber calls"
      pattern: "factory.createCaliber"
    - from: "packages/contracts/Makefile"
      to: "packages/contracts/script/DeployFuji.s.sol"
      via: "forge script invocation"
      pattern: "DeployFuji"
---

<objective>
Create all deployment artifacts for Fuji testnet: MockUSDC contract, deployable MockPriceOracle, the single DeployFuji.s.sol script that deploys everything and configures roles, a Makefile for dry-run and broadcast, and foundry.toml updates.

Purpose: Everything needed to deploy the full protocol to Fuji with one command. After this plan, running `make fuji_deploy` from packages/contracts/ deploys and verifies all contracts.

Output: MockUSDC.sol, MockPriceOracle.sol (in src/), DeployFuji.s.sol, Makefile, updated foundry.toml
</objective>

<execution_context>
@/Users/chiranjibipoudyal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiranjibipoudyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

@packages/contracts/foundry.toml
@packages/contracts/src/AmmoManager.sol
@packages/contracts/src/AmmoFactory.sol
@packages/contracts/src/CaliberMarket.sol
@packages/contracts/src/AmmoToken.sol
@packages/contracts/src/IPriceOracle.sol
@packages/contracts/test/MockPriceOracle.sol
@packages/contracts/test/MockERC20.sol
@packages/shared/src/constants/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MockUSDC, deployable MockPriceOracle, and update foundry.toml</name>
  <files>
    packages/contracts/src/MockUSDC.sol
    packages/contracts/src/MockPriceOracle.sol
    packages/contracts/foundry.toml
  </files>
  <action>
**MockUSDC.sol** (new file at `packages/contracts/src/MockUSDC.sol`):
Create a full ERC20 implementation with these specifics:
- SPDX-License-Identifier: MIT, pragma solidity ^0.8.24
- `name = "Mock USDC"`, `symbol = "USDC"`, `decimals = 6` (all as public constants)
- `FAUCET_CAP = 10_000e6` (10,000 USDC, public constant)
- State: `totalSupply`, `balanceOf`, `allowance` mappings
- Events: `Transfer(from, to, amount)`, `Approval(owner, spender, amount)`
- Custom error: `ExceedsFaucetCap()`
- `faucet(uint256 amount) external` — reverts with ExceedsFaucetCap if `amount > FAUCET_CAP`, otherwise mints to msg.sender and emits Transfer from address(0)
- Full `approve`, `transfer`, `transferFrom` implementations (standard ERC20 logic with allowance deduction, underflow revert via Solidity 0.8 checked math)
- This is NOT OpenZeppelin — hand-roll a minimal ERC20 similar to the existing `test/MockERC20.sol` but with the faucet and constants

**MockPriceOracle.sol** (new file at `packages/contracts/src/MockPriceOracle.sol`):
Copy the existing `test/MockPriceOracle.sol` to `src/MockPriceOracle.sol`. Adjust the import path from `"../src/IPriceOracle.sol"` to `"./IPriceOracle.sol"` since it's now in the same directory. Keep the test version in place — both can coexist. The src/ version will be used by the deploy script for on-chain deployment.

**foundry.toml** (update existing):
Add `evm_version = "cancun"` under `[profile.default]` (after the solc line). Per user decision — Avalanche C-Chain supports cancun opcodes, and this prevents breakage if solc is ever upgraded.

Add `[etherscan]` section:
```toml
[etherscan]
fuji = { key = "${SNOWTRACE_API_KEY}", url = "https://api.avascan.info/v2/network/testnet/evm/43113/etherscan", chain = 43113 }
avalanche = { key = "${SNOWTRACE_API_KEY}", url = "https://api.avascan.info/v2/network/mainnet/evm/43114/etherscan", chain = 43114 }
```
  </action>
  <verify>
Run `cd /Users/chiranjibipoudyal/programming/side_projects/ammo-exchange/packages/contracts && forge build` — must compile without errors. Verify MockUSDC.sol artifact exists in `out/MockUSDC.sol/MockUSDC.json`. Verify `src/MockPriceOracle.sol` exists and compiles.
  </verify>
  <done>
MockUSDC has 6 decimals, public faucet with 10,000 USDC cap. MockPriceOracle in src/ compiles against IPriceOracle. foundry.toml has evm_version = "cancun" and [etherscan] fuji/avalanche entries. `forge build` succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create DeployFuji.s.sol script and Makefile</name>
  <files>
    packages/contracts/script/DeployFuji.s.sol
    packages/contracts/Makefile
  </files>
  <action>
**DeployFuji.s.sol** (new file at `packages/contracts/script/DeployFuji.s.sol`):
Create the `script/` directory if it does not exist. Write a Foundry script that:

1. Imports: `forge-std/Script.sol`, `../src/MockUSDC.sol`, `../src/MockPriceOracle.sol`, `../src/AmmoManager.sol`, `../src/AmmoFactory.sol`
2. Contract `DeployFuji is Script` with a single `function run() external`
3. Inside `run()`:
   - `vm.startBroadcast()` (uses PRIVATE_KEY from env automatically)
   - Deploy `MockUSDC usdc = new MockUSDC()`
   - Deploy `AmmoManager manager = new AmmoManager(msg.sender)` (msg.sender = feeRecipient per constructor)
   - Call `manager.setTreasury(msg.sender)` (deployer = treasury for testnet)
   - Call `manager.setGuardian(msg.sender)` (deployer = guardian for testnet)
   - NOTE: Do NOT call setKeeper — the constructor already sets `keepers[msg.sender] = true`. Do NOT call setFeeRecipient — constructor already sets it via the constructor arg.
   - Deploy `AmmoFactory factory = new AmmoFactory(address(manager), address(usdc), 6)` (6 = USDC decimals)
   - Deploy 4 MockPriceOracle instances with realistic prices and create 4 calibers:
     - `MockPriceOracle oracle9mm = new MockPriceOracle(21e16)` then `factory.createCaliber(bytes32("9MM"), "Ammo 9MM", "MO9MM", address(oracle9mm), 150, 150, 50)`
     - `MockPriceOracle oracle556 = new MockPriceOracle(45e16)` then `factory.createCaliber(bytes32("556"), "Ammo 556", "MO556", address(oracle556), 150, 150, 50)`
     - `MockPriceOracle oracle22lr = new MockPriceOracle(8e16)` then `factory.createCaliber(bytes32("22LR"), "Ammo 22LR", "MO22LR", address(oracle22lr), 150, 150, 100)`
     - `MockPriceOracle oracle308 = new MockPriceOracle(90e16)` then `factory.createCaliber(bytes32("308"), "Ammo 308", "MO308", address(oracle308), 150, 150, 20)`
     - Fee params: 150 (mintFeeBps), 150 (redeemFeeBps) for all. minMintRounds: 50, 50, 100, 20 respectively (from CALIBER_SPECS in shared/constants)
   - After each createCaliber, capture the returned (market, token) addresses
   - `vm.stopBroadcast()`
   - After stopBroadcast, log ALL deployed addresses using `console.log`:
     ```
     console.log("=== Deployed Addresses ===");
     console.log("MockUSDC:", address(usdc));
     console.log("AmmoManager:", address(manager));
     console.log("AmmoFactory:", address(factory));
     console.log("--- 9MM ---");
     console.log("Market:", market9mm);
     console.log("Token:", token9mm);
     // ... repeat for 556, 22LR, 308
     ```
   - This output is critical — the user will copy these addresses into shared config in plan 01-02.

**Makefile** (new file at `packages/contracts/Makefile`):
```makefile
-include .env

fuji_check:
	@forge clean
	@forge script script/DeployFuji.s.sol:DeployFuji \
		--rpc-url $(FUJI_RPC_URL) \
		--private-key $(PRIVATE_KEY) -vv

fuji_deploy:
	@forge clean
	@forge script script/DeployFuji.s.sol:DeployFuji \
		--rpc-url $(FUJI_RPC_URL) \
		--private-key $(PRIVATE_KEY) \
		--broadcast --verify --slow \
		--verifier-url "https://api.avascan.info/v2/network/testnet/evm/43113/etherscan" \
		--etherscan-api-key $(SNOWTRACE_API_KEY) -vv
```

NOTE: The Makefile uses tabs for indentation (mandatory for Make). The `-include .env` loads env vars from the contracts-level .env (or the root .env if symlinked). Use `@` prefix to suppress command echo.
  </action>
  <verify>
Run `cd /Users/chiranjibipoudyal/programming/side_projects/ammo-exchange/packages/contracts && forge build` — must compile including the new script. Verify `script/DeployFuji.s.sol` exists. Verify `Makefile` exists with both targets. Run `grep -c "fuji_check\|fuji_deploy" Makefile` — should return 2.
  </verify>
  <done>
DeployFuji.s.sol deploys MockUSDC, 4 MockPriceOracles, AmmoManager (with treasury+guardian set), AmmoFactory, and 4 calibers in sequence. All addresses are logged via console.log. Makefile has fuji_check and fuji_deploy targets. forge build compiles cleanly.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Deploy contracts to Fuji testnet</name>
  <files>packages/contracts/.env</files>
  <action>
Human action required: Deploy the contracts to Fuji testnet using the Makefile targets created in Task 2.

1. Ensure `.env` file (at monorepo root or `packages/contracts/.env`) has:
   - `PRIVATE_KEY=0x...` (Fuji testnet wallet with AVAX for gas — get free AVAX from https://faucet.avax.network)
   - `SNOWTRACE_API_KEY=...` (from routescan.io, or try `verifyContract` as placeholder)
   - `FUJI_RPC_URL=https://api.avax-test.network/ext/bc/C/rpc`

2. From `packages/contracts/`, run dry-run: `make fuji_check`
   Expected: Simulation succeeds with console.log output showing deployed addresses. No reverts.

3. If dry-run succeeds, deploy for real: `make fuji_deploy`
   Expected: All contracts deployed and verification attempted. Copy ALL deployed addresses from the output.

4. Verify on Snowtrace Fuji (https://testnet.snowtrace.io) — search deployer address to see contract creation txs.

5. Share the deployed addresses output so they can be entered into shared config in plan 01-02.
  </action>
  <verify>All contracts deployed to Fuji. Console output shows non-zero addresses for MockUSDC, AmmoManager, AmmoFactory, and all 4 caliber markets + tokens. Snowtrace shows contract creation transactions from the deployer address.</verify>
  <done>Contracts are live on Fuji testnet. Deployed addresses are captured and ready to be entered into shared config. Paste the addresses output and type "deployed" to continue to plan 01-02.</done>
</task>

</tasks>

<verification>
1. `forge build` compiles all contracts including MockUSDC, MockPriceOracle (src/), and DeployFuji.s.sol without errors
2. `make fuji_check` runs the deployment simulation successfully (no reverts)
3. MockUSDC has `decimals() == 6` and `FAUCET_CAP == 10_000e6`
4. DeployFuji.s.sol sets treasury and guardian to msg.sender (does NOT redundantly set keeper or feeRecipient)
5. All 4 calibers are created with correct fee params (150 bps mint, 150 bps redeem) and correct minMintRounds (50, 50, 100, 20)
</verification>

<success_criteria>
1. All Solidity files compile cleanly with `forge build`
2. Deployment simulation (`make fuji_check`) completes without revert
3. Contracts are deployed to Fuji and addresses are captured from console output
4. Contracts are verified on Snowtrace Fuji (or verification was attempted)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
