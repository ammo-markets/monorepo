---
phase: 25-slide-content-and-navigation
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - apps/pitchdeck/lib/slideData.ts
  - apps/pitchdeck/components/slides/SlideCover.tsx
  - apps/pitchdeck/components/slides/SlideProblem.tsx
  - apps/pitchdeck/components/slides/SlideVolatility.tsx
  - apps/pitchdeck/components/slides/SlideSolution.tsx
  - apps/pitchdeck/components/slides/SlideHowItWorks.tsx
  - apps/pitchdeck/components/slides/SlideMarket.tsx
  - apps/pitchdeck/components/slides/SlideCompetitive.tsx
  - apps/pitchdeck/components/slides/SlideRevenue.tsx
  - apps/pitchdeck/components/slides/SlideTraction.tsx
  - apps/pitchdeck/components/slides/SlideRegulatory.tsx
  - apps/pitchdeck/components/slides/SlideRoadmap.tsx
  - apps/pitchdeck/components/slides/SlideTeam.tsx
  - apps/pitchdeck/components/slides/SlideAsk.tsx
  - apps/pitchdeck/components/slides/index.ts
  - apps/pitchdeck/components/SlideLayout.tsx
autonomous: true

must_haves:
  truths:
    - "All 13 slides render with their complete content (cover, problem, price chart, solution, how it works, market, competitive, revenue, traction, regulatory, roadmap, team, ask)"
    - "SLIDE-03 shows an interactive 9mm price volatility chart with labeled axes and data points from 2018-2025"
    - "Pressing ArrowRight/ArrowLeft navigates through all 13 slides with smooth CSS transitions"
    - "Slide counter shows '1 / 13' through '13 / 13' as user navigates"
    - "All slide content uses hex-only colors (no oklch anywhere)"
  artifacts:
    - path: "apps/pitchdeck/lib/slideData.ts"
      provides: "Static data constants for all slides (price data, market stats, fee table, roadmap phases, team bios)"
    - path: "apps/pitchdeck/components/SlideLayout.tsx"
      provides: "Reusable slide layout wrapper with consistent padding and typography"
    - path: "apps/pitchdeck/components/slides/SlideVolatility.tsx"
      provides: "Recharts AreaChart with hex colors for 9mm price history"
      contains: "recharts"
    - path: "apps/pitchdeck/components/slides/index.ts"
      provides: "SLIDES array with all 13 slide components in order"
      exports: ["SLIDES"]
  key_links:
    - from: "apps/pitchdeck/components/slides/SlideVolatility.tsx"
      to: "recharts"
      via: "direct import with hex color props"
      pattern: "stroke=\"#c6a44e\""
    - from: "apps/pitchdeck/components/slides/SlideHowItWorks.tsx"
      to: "@ammo-exchange/shared"
      via: "CALIBER_SPECS import for caliber data"
      pattern: "CALIBER_SPECS"
    - from: "apps/pitchdeck/components/slides/index.ts"
      to: "apps/pitchdeck/components/PitchDeck.tsx"
      via: "SLIDES array consumed by PitchDeck orchestrator"
      pattern: "SLIDES"
---

<objective>
Create all 13 slide content components with their static data, a shared SlideLayout wrapper, and a centralized slideData.ts for data constants. Update the SLIDES barrel export to include all 13 components. Replace the placeholder SlideCover with full content.

Purpose: Completes the investor pitch deck with all slide content -- the core deliverable of Phase 25.
Output: 13 fully rendered slides navigable via the slide system from Plan 25-01.
</objective>

<execution_context>
@/Users/chiranjibipoudyal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiranjibipoudyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-slide-content-and-navigation/25-RESEARCH.md
@.planning/phases/25-slide-content-and-navigation/25-01-SUMMARY.md
@apps/pitchdeck/app/globals.css
@apps/pitchdeck/lib/useDeck.ts
@apps/pitchdeck/components/PitchDeck.tsx
@packages/shared/src/constants/index.ts
@whitepaper.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SlideLayout, slideData.ts, and first 7 slides (Cover through Competitive)</name>
  <files>
    apps/pitchdeck/components/SlideLayout.tsx
    apps/pitchdeck/lib/slideData.ts
    apps/pitchdeck/components/slides/SlideCover.tsx
    apps/pitchdeck/components/slides/SlideProblem.tsx
    apps/pitchdeck/components/slides/SlideVolatility.tsx
    apps/pitchdeck/components/slides/SlideSolution.tsx
    apps/pitchdeck/components/slides/SlideHowItWorks.tsx
    apps/pitchdeck/components/slides/SlideMarket.tsx
    apps/pitchdeck/components/slides/SlideCompetitive.tsx
  </files>
  <action>
    1. Create `apps/pitchdeck/components/SlideLayout.tsx`:
       - Reusable wrapper for all slides: flex flex-col h-full w-full p-12
       - Accept `children` and optional `className` prop
       - Provides consistent spacing and typography context

    2. Create `apps/pitchdeck/lib/slideData.ts` with ALL static data for ALL 13 slides:
       - `PRICE_DATA`: 9mm FMJ price history array with ~8-10 data points covering 2018-2025 (yearly, with extra points at pandemic spike). Use data from whitepaper section 1.2: baseline ~$0.17-0.18, spike to ~$0.82 in 2021, recovery to ~$0.21-0.22
       - `MARKET_STATS`: TAM $8B, SAM $800M (10%), SOM $80M (1%), gun ownership 32%
       - `FEE_TABLE`: mint 1.5%, redeem 1.5%, wholesale spread 5-15%
       - `ROADMAP_PHASES`: 4 phases (MVP, Expansion, DeFi Integration, Scale) with descriptions and timelines from whitepaper section 9
       - `TEAM_MEMBERS`: 3-4 placeholder team members with roles (Founder, CTO, Head of Compliance, etc.) and placeholder bios
       - `COMPETITIVE_DATA`: comparison entries for AmmoSeek, AmmoSquared, Forums/Discord, Ammo Exchange
       - `PROBLEM_STATS`: key pain point statistics ($8B market, no price instrument, etc.)
       - All data typed with TypeScript interfaces
       - Import FEES from @ammo-exchange/shared for fee constants where applicable

    3. Create/update slide components 1-7 (all use SlideLayout wrapper):

       **SlideCover.tsx (SLIDE-01)** -- Replace placeholder from 25-01:
       - Large "Ammo Exchange" heading in brass
       - Tagline: "Make Your Ammo Liquid"
       - Subtext: "Tokenized ammunition trading on Avalanche"
       - 5-second hook: "The first DeFi protocol for ammunition price exposure"
       - Clean centered layout, dark background

       **SlideProblem.tsx (SLIDE-02)**:
       - Title: "The Problem"
       - Present 3-4 pain points with icons/visual indicators:
         - $8B ammunition market with no financial instruments
         - Price volatility with no hedge mechanism (reference 9mm 355% spike)
         - Geographic restrictions limit access
         - Fragmented market, no transparent pricing
       - Each pain point as a card or list item with brass accent

       **SlideVolatility.tsx (SLIDE-03)**:
       - "use client" directive (required for Recharts)
       - Title: "9mm Price Volatility"
       - Recharts AreaChart with PRICE_DATA from slideData.ts
       - ALL colors as hex literals: stroke="#c6a44e", fill gradient from brass, grid "#1a1a25", axis "#8a8a9a"
       - Tooltip with dark background (#12121a) and border (#1a1a25)
       - Use ResponsiveContainer with explicit parent height (h-[350px] or similar)
       - Annotation or callout for the pandemic spike: "355% increase"
       - DO NOT import any shadcn chart wrapper or use CSS variables for colors

       **SlideSolution.tsx (SLIDE-04)**:
       - Title: "The Solution"
       - 3-step visual: USDC In -> Tokens Out -> Redeem for Physical
       - Each step as a card with icon, title, description
       - Highlight: "Anyone worldwide can get price exposure"
       - Mention: "Only verified US residents can redeem for physical delivery"

       **SlideHowItWorks.tsx (SLIDE-05)**:
       - Title: "How It Works"
       - 2-step async flow: User initiates -> Admin finalizes
       - Show mint flow and redeem flow side by side or sequential
       - Per-caliber tokens: import and display 4 calibers from CALIBER_SPECS
       - Show: 1 token = 1 round, 18 decimals, ERC20 standard

       **SlideMarket.tsx (SLIDE-06)**:
       - Title: "Market Opportunity"
       - TAM/SAM/SOM visual (concentric circles or tiered bars)
       - TAM: $8B US ammunition market
       - SAM: $800M online/accessible segment (10%)
       - SOM: $80M initial target (1%)
       - Supporting stat: 32% US households own firearms, 44% of gun owners buy ammo regularly

       **SlideCompetitive.tsx (SLIDE-07)**:
       - Title: "Competitive Landscape"
       - Positioning: "PAXG for Ammunition"
       - Comparison table or grid: AmmoSeek (price comparison only), AmmoSquared (layaway program), Forums (P2P, no guarantees), Ammo Exchange (tokenized, DeFi-native, global access)
       - Highlight differentiators: blockchain settlement, global access, price exposure without physical storage
  </action>
  <verify>
    - `pnpm --filter @ammo-exchange/pitchdeck check` passes
    - All 7 slide files exist and export default components
    - `grep -r "oklch" apps/pitchdeck/components/slides/` returns nothing
    - SlideVolatility.tsx contains "recharts" import and hex color strings
  </verify>
  <done>SlideLayout wrapper created. slideData.ts contains all static data for 13 slides. Slides 1-7 render complete content with proper styling, hex-only colors, and Recharts chart on SLIDE-03.</done>
</task>

<task type="auto">
  <name>Task 2: Create slides 8-13 (Revenue through Ask) and wire SLIDES barrel export</name>
  <files>
    apps/pitchdeck/components/slides/SlideRevenue.tsx
    apps/pitchdeck/components/slides/SlideTraction.tsx
    apps/pitchdeck/components/slides/SlideRegulatory.tsx
    apps/pitchdeck/components/slides/SlideRoadmap.tsx
    apps/pitchdeck/components/slides/SlideTeam.tsx
    apps/pitchdeck/components/slides/SlideAsk.tsx
    apps/pitchdeck/components/slides/index.ts
  </files>
  <action>
    1. Create slide components 8-13 (all use SlideLayout wrapper):

       **SlideRevenue.tsx (SLIDE-08)**:
       - Title: "Revenue Model"
       - Fee structure table: Mint fee 1.5%, Redeem fee 1.5%, max 5% hard cap
       - Unit economics example: $10,000 mint order -> $150 protocol fee
       - Wholesale spread: 5-15% on procurement
       - Import FEES from @ammo-exchange/shared for BPS values if available, or use slideData.ts constants
       - Style as a clean table or card grid

       **SlideTraction.tsx (SLIDE-09)**:
       - Title: "Traction & Demo"
       - Live testnet status: "Deployed on Avalanche Fuji"
       - Stats: 13 contracts deployed, 4 calibers, full mint/redeem flow
       - CTA button/link: "Try the Live Demo" pointing to the testnet app (use placeholder URL like https://ammo.exchange or note as "Coming Soon")
       - Visual: list of achievements/milestones reached

       **SlideRegulatory.tsx (SLIDE-10)**:
       - Title: "Regulatory Positioning"
       - Key points with green checkmarks:
         - No FFL required for token trading (tokens are not ammunition)
         - KYC/identity verification required at redemption only
         - Restricted states enforced (CA, NY, IL, DC, NJ)
         - Token classification: utility token for commodity access, not a security
       - Import RESTRICTED_STATES from @ammo-exchange/shared if available, or hardcode

       **SlideRoadmap.tsx (SLIDE-11)**:
       - Title: "Roadmap"
       - 4-phase timeline from ROADMAP_PHASES in slideData.ts:
         - Phase 1: MVP (Fuji testnet, 4 calibers, basic UI) -- CURRENT
         - Phase 2: Expansion (mainnet launch, more calibers, Uniswap pools)
         - Phase 3: DeFi Integration (price oracles, lending, yield)
         - Phase 4: Scale (institutional, international expansion)
       - Visual timeline with current phase highlighted in brass

       **SlideTeam.tsx (SLIDE-12)**:
       - Title: "Team"
       - 3-4 placeholder team member cards from TEAM_MEMBERS in slideData.ts
       - Each card: name, role, 1-line bio placeholder
       - Clean grid layout (2x2 or horizontal row)
       - Placeholder avatar circles with initials

       **SlideAsk.tsx (SLIDE-13)**:
       - Title: "The Ask"
       - General CTA for investors and partners
       - Key points: seeking strategic partners, early investors, industry advisors
       - Contact info placeholder
       - Closing tagline: "Make Your Ammo Liquid"
       - Brass-accented CTA styling

    2. Update `apps/pitchdeck/components/slides/index.ts`:
       - Import all 13 slide components
       - Export SLIDES array in order: [SlideCover, SlideProblem, SlideVolatility, SlideSolution, SlideHowItWorks, SlideMarket, SlideCompetitive, SlideRevenue, SlideTraction, SlideRegulatory, SlideRoadmap, SlideTeam, SlideAsk]
       - Type as `ComponentType[]` from React

    3. Verify the full system works end-to-end:
       - Run `pnpm --filter @ammo-exchange/pitchdeck check`
       - Run `pnpm --filter @ammo-exchange/pitchdeck build`
       - Confirm SLIDES.length === 13
  </action>
  <verify>
    - `pnpm --filter @ammo-exchange/pitchdeck check` passes
    - `pnpm --filter @ammo-exchange/pitchdeck build` succeeds (static export)
    - All 13 slide files exist in apps/pitchdeck/components/slides/
    - `grep -c "Slide" apps/pitchdeck/components/slides/index.ts` shows 13 imports
    - `grep -r "oklch" apps/pitchdeck/components/` returns nothing
    - Running dev server shows all 13 slides navigable with keyboard and click controls
  </verify>
  <done>All 13 slides render with complete content. SLIDES barrel export contains all 13 components. Slide counter shows "1 / 13" through "13 / 13". ArrowRight/ArrowLeft navigates through all slides with CSS transitions. Static build succeeds.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @ammo-exchange/pitchdeck check` -- TypeScript compiles without errors
2. `pnpm --filter @ammo-exchange/pitchdeck build` -- static export succeeds
3. All 13 slide files exist in apps/pitchdeck/components/slides/
4. SLIDES array in index.ts has exactly 13 entries
5. Navigate localhost:3001 -- all slides render with content, no blank slides
6. ArrowRight from slide 1 through 13 -- each slide has unique content
7. ArrowLeft at slide 1 -- no action (boundary respected)
8. Slide counter shows correct position (1/13 through 13/13)
9. SLIDE-03 (Volatility) shows Recharts area chart with visible data
10. `grep -r "oklch" apps/pitchdeck/` returns nothing (hex-only confirmed)
</verification>

<success_criteria>
- All 13 slides render with complete, meaningful investor deck content
- SlideVolatility (SLIDE-03) shows an interactive Recharts AreaChart with 9mm price data using only hex colors
- SlideHowItWorks (SLIDE-05) displays caliber data from @ammo-exchange/shared
- Navigation works through all 13 slides with correct counter and progress bar
- CSS slide transitions animate smoothly between all slides
- Static build produces a working export with all 13 slides
- Zero oklch colors anywhere in the pitchdeck app
</success_criteria>

<output>
After completion, create `.planning/phases/25-slide-content-and-navigation/25-02-SUMMARY.md`
</output>
