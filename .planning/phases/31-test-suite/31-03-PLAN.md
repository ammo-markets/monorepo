---
phase: 31-test-suite
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/test/CaliberMarket.t.sol
autonomous: true

must_haves:
  truths:
    - "Mint initiation happy path: user approves USDC, calls startMint, order is created with correct fields"
    - "Redeem initiation happy path: user approves tokens, calls startRedeem, tokens are locked in market"
    - "Keeper finalization happy path: keeper calls finalizeMint with valid price, user receives tokens, USDC goes to treasury"
  artifacts:
    - path: "packages/contracts/test/CaliberMarket.t.sol"
      provides: "E2E flow tests for mint initiation, redeem initiation, and keeper finalization"
      min_lines: 600
  key_links:
    - from: "packages/contracts/test/CaliberMarket.t.sol"
      to: "packages/contracts/src/CaliberMarket.sol"
      via: "Foundry test import"
      pattern: "import.*CaliberMarket"
---

<objective>
Verify existing Foundry tests cover the E2E happy paths required by TEST-05, and add any missing coverage.

Purpose: The existing CaliberMarket.t.sol already has extensive tests. This plan verifies TEST-05 coverage exists and adds a clearly labeled E2E section grouping the happy-path assertions into a discoverable test block.
Output: Updated Foundry test file with explicit TEST-05 labeled E2E tests.
</objective>

<execution_context>
@/Users/chiranjibipoudyal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiranjibipoudyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/contracts/test/CaliberMarket.t.sol
@packages/contracts/src/CaliberMarket.sol
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit E2E happy-path test section for TEST-05 coverage</name>
  <files>packages/contracts/test/CaliberMarket.t.sol</files>
  <action>
Review the existing CaliberMarket.t.sol tests. The following TEST-05 requirements already have coverage:

- Mint initiation: `testStartMintCreatesOrder` covers USDC approval + startMint + order field verification
- Keeper finalization with valid price: `testFinalizeMintUsesActualPrice` covers finalizeMint + token minting + fee distribution
- Redeem initiation: `testRedeemFlowBurnsAndFees` covers token approval + startRedeem + finalizeRedeem

Add a NEW clearly labeled section at the end of the test contract (before the Helpers section) with these tests:

```
// ===============================================
// -- TEST-05: E2E Happy Path Flows
// ===============================================
```

1. **`testE2EMintInitiationHappyPath()`**: Full mint initiation flow in one test:
   - User starts with 1000 USDC
   - User approves market for 200 USDC
   - User calls startMint(200e6, 500, 0) -- 5% slippage, no deadline
   - Assert: user USDC balance decreased by 200e6
   - Assert: market USDC balance is 200e6
   - Assert: mint order status is Started
   - Assert: order stores correct usdcAmount, user address, requestPrice

2. **`testE2ERedeemInitiationHappyPath()`**: Full redeem initiation flow:
   - Mint tokens to user first (using \_mintTokensToUser helper)
   - User approves market for 200e18 tokens
   - User calls startRedeem(200e18, 0) -- no deadline
   - Assert: tokens transferred from user to market (user balance decreased, market balance increased)
   - Assert: redeem order status is Requested
   - Assert: order stores correct tokenAmount and user address

3. **`testE2EKeeperFinalizationHappyPath()`**: Full finalization with valid price:
   - Create and finalize a mint order end-to-end
   - Assert: user received minted tokens (balance > 0)
   - Assert: fee recipient received fee USDC
   - Assert: treasury received net USDC
   - Assert: market USDC balance is 0 (all distributed)
   - Assert: mint order status is Finalized

These tests are deliberately simple and happy-path-only. They complement the existing granular tests by serving as readable end-to-end verification of the core protocol flows.

Keep all existing tests unchanged. Add the new section after the "Pause: irreversible blocked, unwind allowed" section and before "Helpers".
</action>
<verify>Run `cd /Users/chiranjibipoudyal/programming/side_projects/ammo-exchange/packages/contracts && forge test --match-test "testE2E" -vvv` -- all 3 E2E tests pass.</verify>
<done>Three explicit E2E happy-path tests pass, covering mint initiation, redeem initiation, and keeper finalization flows.</done>
</task>

</tasks>

<verification>
1. `cd /Users/chiranjibipoudyal/programming/side_projects/ammo-exchange/packages/contracts && forge test` -- all tests pass (existing + new)
2. `forge test --match-test "testE2E" -vvv` -- 3 new E2E tests pass with verbose output
3. No existing tests broken
</verification>

<success_criteria>

- TEST-05 covered: E2E flow tests for mint initiation (USDC approval + startMint), redeem initiation (token approval + startRedeem), and keeper finalization (finalizeMint with valid price)
- All 3 new E2E tests pass
- All existing contract tests still pass (no regressions)
  </success_criteria>

<output>
After completion, create `.planning/phases/31-test-suite/31-03-SUMMARY.md`
</output>
