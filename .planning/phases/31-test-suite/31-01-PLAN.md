---
phase: 31-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/worker/src/__tests__/handlers.test.ts
  - apps/worker/src/__tests__/helpers.ts
  - apps/worker/package.json
  - apps/worker/tsconfig.json
autonomous: true

must_haves:
  truths:
    - "Processing the same MintStarted event twice (identical txHash + logIndex) produces exactly one order record"
    - "Processing the same RedeemRequested event twice (identical txHash + logIndex) produces exactly one order record"
    - "Two MintStarted events in the same tx (same txHash, different logIndex) produce two distinct order records"
    - "Two RedeemRequested events in the same tx (same txHash, different logIndex) produce two distinct order records"
  artifacts:
    - path: "apps/worker/src/__tests__/handlers.test.ts"
      provides: "Worker handler unit tests for idempotency and composite uniqueness"
      min_lines: 80
    - path: "apps/worker/src/__tests__/helpers.ts"
      provides: "Test helpers: mock PrismaTx factory, EventMeta builders"
      min_lines: 30
  key_links:
    - from: "apps/worker/src/__tests__/handlers.test.ts"
      to: "apps/worker/src/handlers/mint.ts"
      via: "direct import"
      pattern: "import.*handleMintStarted.*from.*handlers/mint"
    - from: "apps/worker/src/__tests__/handlers.test.ts"
      to: "apps/worker/src/handlers/redeem.ts"
      via: "direct import"
      pattern: "import.*handleRedeemRequested.*from.*handlers/redeem"
---

<objective>
Worker handler unit tests verifying idempotent replay and composite uniqueness for order creation.

Purpose: Prove that the Phase 27 audit remediation (composite txHash+logIndex dedup) works correctly -- same event replayed = 1 order, two events in one tx = 2 orders.
Output: Passing Bun test suite for worker event handlers.
</objective>

<execution_context>
@/Users/chiranjibipoudyal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiranjibipoudyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/worker/src/handlers/mint.ts
@apps/worker/src/handlers/redeem.ts
@apps/worker/src/lib/constants.ts
@apps/worker/src/lib/cursor.ts
@packages/db/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test helpers and mock Prisma transaction factory</name>
  <files>apps/worker/src/__tests__/helpers.ts, apps/worker/package.json, apps/worker/tsconfig.json</files>
  <action>
Create `apps/worker/src/__tests__/helpers.ts` with:

1. A `createMockPrismaTx()` factory that returns an object satisfying the `PrismaTx` type from `../lib/cursor`. It must have:
   - `order.upsert` -- tracks calls in an array, simulates Prisma upsert behavior (stores in an in-memory Map keyed by composite `txHash_logIndex`)
   - `order.updateMany` -- returns `{ count: 0 }` by default (no pending order), configurable
   - `order.findFirst` -- returns null by default, configurable
   - `activityLog.create` -- no-op, tracks calls
   - `blockCursor.findUnique` / `blockCursor.upsert` -- minimal stubs

2. A `buildEventMeta()` helper that creates an `EventMeta` object with sensible defaults:
   - address: use first market address from CONTRACT_ADDRESSES.fuji.calibers (the 9mm market address)
   - transactionHash: accept override, default to a deterministic `0xabc...` hash
   - blockNumber: accept override, default to `100n`
   - logIndex: accept override, default to `0`

3. Helper builders for `MintStartedArgs` and `RedeemRequestedArgs` with sensible defaults.

The mock PrismaTx must track all upsert calls so tests can assert call count and arguments. Use a simple in-memory store (Map or array) -- NOT a real database.

Update `apps/worker/tsconfig.json` to include `src/__tests__` in the `include` array (change `["src"]` to `["src/**/*"]` or add the test path).

No additional test dependencies needed -- Bun has a built-in test runner (`bun:test`).
</action>
<verify>Run `cd /Users/chiranjibipoudyal/programming/side_projects/ammo-exchange && pnpm --filter @ammo-exchange/worker check` -- typecheck passes with new test files.</verify>
<done>Mock PrismaTx factory and event builders exist and typecheck cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Write handler tests for idempotency and composite uniqueness</name>
  <files>apps/worker/src/__tests__/handlers.test.ts</files>
  <action>
Create `apps/worker/src/__tests__/handlers.test.ts` using `bun:test` (`describe`, `test`, `expect`).

Import `handleMintStarted`, `handleMintFinalized` from `../handlers/mint` and `handleRedeemRequested`, `handleRedeemFinalized` from `../handlers/redeem`. Import helpers from `./helpers`.

Write these test groups:

**describe("handleMintStarted - idempotency (TEST-01)"):**

1. "processes event once -- creates one order": Call handleMintStarted with a mock tx. Assert upsert was called once with correct txHash_logIndex composite key.
2. "replaying same event -- still results in one order": Call handleMintStarted twice with identical (txHash, logIndex). Assert the mock store contains exactly 1 entry for that composite key. The second call should hit the upsert `update: {}` path (no-op).
3. "replay with same txHash but different logIndex -- creates two orders (TEST-02)": Call handleMintStarted twice: first with logIndex=0, second with logIndex=1, same txHash. Assert mock store has 2 distinct entries.

**describe("handleRedeemRequested - idempotency (TEST-01)"):** 4. "processes event once -- creates one order": Same pattern as mint. 5. "replaying same event -- still results in one order": Same pattern as mint. 6. "same txHash different logIndex -- creates two orders (TEST-02)": Same pattern as mint.

**describe("handleMintStarted - data correctness"):** 7. "stores usdcAmount from event args": Assert the upsert create data has `usdcAmount: args.usdcAmount.toString()`. 8. "stores caliber resolved from contract address": Assert `caliber` field matches expected Prisma caliber for the meta.address used.

**describe("handleRedeemRequested - data correctness"):** 9. "stores tokenAmount from event args": Assert the upsert create data has `tokenAmount: args.tokenAmount.toString()`.

For the mock PrismaTx, the upsert mock should:

- On first call with a given composite key: store the `create` payload
- On subsequent calls with the same composite key: apply the `update` payload (which is `{}`, so no change)
- This accurately simulates Prisma's upsert semantics

Run tests with: `cd apps/worker && bun test`
</action>
<verify>Run `cd /Users/chiranjibipoudyal/programming/side_projects/ammo-exchange/apps/worker && bun test` -- all tests pass.</verify>
<done>9+ handler tests pass, proving: (a) same event twice = 1 order, (b) same txHash different logIndex = 2 orders, (c) correct amount fields populated.</done>
</task>

</tasks>

<verification>
1. `cd /Users/chiranjibipoudyal/programming/side_projects/ammo-exchange/apps/worker && bun test` -- all tests pass with 0 failures
2. `pnpm --filter @ammo-exchange/worker check` -- typecheck still passes
3. Test output shows idempotency assertions (same event = 1 order) and composite uniqueness assertions (different logIndex = 2 orders)
</verification>

<success_criteria>

- TEST-01 covered: Idempotent replay test for both MintStarted and RedeemRequested handlers
- TEST-02 covered: Composite uniqueness test showing two events in one tx produce two distinct orders
- All tests pass with `bun test`
- No regressions in worker typecheck
  </success_criteria>

<output>
After completion, create `.planning/phases/31-test-suite/31-01-SUMMARY.md`
</output>
