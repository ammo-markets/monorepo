---
phase: 31-test-suite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/api/__tests__/auth.test.ts
  - apps/web/app/api/__tests__/compliance.test.ts
  - apps/web/app/api/__tests__/helpers.ts
  - apps/web/package.json
  - apps/web/tsconfig.json
autonomous: true

must_haves:
  truths:
    - "Unauthenticated requests to /api/orders return 401"
    - "Unauthenticated requests to /api/users/kyc return 401"
    - "Non-keeper authenticated requests to /api/admin/orders return 403"
    - "A user in a restricted state is rejected for redeem shipping"
    - "State code 'ca' is normalized to 'CA' in KYC validation"
    - "KYC GET response contains masked gov ID (****XXXX format), never raw"
  artifacts:
    - path: "apps/web/app/api/__tests__/auth.test.ts"
      provides: "API auth tests for session enforcement and keeper gating"
      min_lines: 60
    - path: "apps/web/app/api/__tests__/compliance.test.ts"
      provides: "API compliance tests for state validation, KYC masking"
      min_lines: 60
    - path: "apps/web/app/api/__tests__/helpers.ts"
      provides: "Test helpers: mock session, mock Prisma, request builders"
      min_lines: 40
  key_links:
    - from: "apps/web/app/api/__tests__/auth.test.ts"
      to: "apps/web/lib/auth.ts"
      via: "mocked session functions"
      pattern: "mock.*requireSession|requireKeeper"
    - from: "apps/web/app/api/__tests__/compliance.test.ts"
      to: "apps/web/app/api/users/kyc/route.ts"
      via: "direct route handler import"
      pattern: "import.*GET.*POST.*from.*kyc/route"
---

<objective>
API auth and compliance tests verifying session enforcement, keeper gating, restricted-state rejection, state normalization, and KYC data masking.

Purpose: Prove Phase 29 security hardening works -- unauthenticated users get 401, non-keepers get 403 on admin routes, restricted states are rejected, and gov IDs are masked.
Output: Passing test suite for API route handlers.
</objective>

<execution_context>
@/Users/chiranjibipoudyal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiranjibipoudyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/lib/auth.ts
@apps/web/app/api/orders/route.ts
@apps/web/app/api/admin/orders/route.ts
@apps/web/app/api/users/kyc/route.ts
@apps/web/app/api/redeem/shipping/route.ts
@packages/shared/src/constants/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test helpers and install vitest for Next.js API testing</name>
  <files>apps/web/app/api/__tests__/helpers.ts, apps/web/package.json, apps/web/tsconfig.json</files>
  <action>
Install vitest as a dev dependency in the web app: `pnpm --filter @ammo-exchange/web add -D vitest`.

Add a test script to `apps/web/package.json`: `"test": "vitest run"`.

Create `apps/web/app/api/__tests__/helpers.ts` with:

1. **Mock module setup instructions** -- tests will use `vi.mock()` to mock:
   - `@/lib/auth` -- mock `requireSession` and `requireKeeper` to control auth state
   - `@ammo-exchange/db` -- mock `prisma` object with chainable query methods
   - `@/lib/viem` -- mock `publicClient` (needed by requireKeeper's isKeeper check)

2. **`createMockSession(address?: string)`** -- returns `{ address: "0xbeef...", chainId: 43113 }` or throws Response(401) if no address.

3. **`createMockPrisma()`** -- returns a mock Prisma client with:
   - `user.findUnique` -- returns configurable user object
   - `user.upsert` -- returns configurable user object
   - `order.findUnique` -- returns configurable order object
   - `order.findMany` / `order.count` -- return configurable results
   - `shippingAddress.upsert` -- returns configurable result

4. **`buildRequest(url: string, options?: RequestInit)`** -- creates a NextRequest-like object for testing route handlers. Use `new Request(url, options)` since Next.js route handlers accept standard Request objects.

Update `apps/web/tsconfig.json` to ensure test files are included. The existing `include` should cover `app/**/*` already -- verify this. If test files would be excluded, add the test directory pattern.

NOTE: Since Next.js route handlers use module-level imports (`import { prisma } from "@ammo-exchange/db"`), tests MUST use `vi.mock()` at the top of test files to intercept these imports BEFORE the route module loads. This is a vitest hoisting requirement.
  </action>
  <verify>Run `cd /Users/chiranjibipoudyal/programming/side_projects/ammo-exchange && pnpm --filter @ammo-exchange/web check` -- typecheck passes.</verify>
  <done>Vitest installed, helpers created, typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Write auth tests (TEST-03) and compliance tests (TEST-04)</name>
  <files>apps/web/app/api/__tests__/auth.test.ts, apps/web/app/api/__tests__/compliance.test.ts</files>
  <action>
**File: `apps/web/app/api/__tests__/auth.test.ts`**

Use `vi.mock("@/lib/auth")` to control auth behavior. Import route handlers directly from their route.ts files.

**describe("API Auth - Protected routes (TEST-03)"):**

1. "GET /api/orders returns 401 when unauthenticated": Mock `requireSession` to throw Response(401). Import GET from `../orders/route`. Call `GET(request)`. Assert response.status === 401.

2. "GET /api/users/kyc returns 401 when unauthenticated": Same pattern with KYC GET handler.

3. "POST /api/users/kyc returns 401 when unauthenticated": Same pattern with KYC POST handler.

4. "GET /api/admin/orders returns 401 when unauthenticated": Mock `requireKeeper` to throw Response(401). Import GET from `../admin/orders/route`. Assert 401.

5. "GET /api/admin/orders returns 403 for non-keeper": Mock `requireKeeper` to throw Response(403, "Forbidden: not a keeper"). Assert 403.

NOTE: The admin routes call `requireKeeper()` which internally calls `requireSession()` first. The mock should simulate the full chain:
- No session: throws 401
- Session but not keeper: throws 403
- Since admin uses `requireKeeper`, mock it directly to throw the appropriate response.

IMPORTANT: The admin routes return 403 (not 404) for non-keepers per the actual implementation in `auth.ts`. The requirement says "404" but the actual code throws 403. Test against the actual behavior (403). Add a comment noting: "Requirement TEST-03 says 404 for non-keeper admin, but implementation returns 403 per auth.ts requireKeeper(). Testing actual behavior."

---

**File: `apps/web/app/api/__tests__/compliance.test.ts`**

Mock `@/lib/auth` to return a valid session. Mock `@ammo-exchange/db` prisma.

**describe("KYC data masking (TEST-04)"):**

1. "GET /api/users/kyc masks govIdNumber to last 4 chars": Mock prisma.user.findUnique to return a user with `kycGovIdNumber: "DL123456789"`. Call KYC GET handler. Assert response body has `kycGovIdNumber: "****6789"` (never the full number).

2. "GET /api/users/kyc returns null govIdNumber when user has none": Mock user with null kycGovIdNumber. Assert response has `kycGovIdNumber: null`.

**describe("State code validation (TEST-04)"):**

3. "POST /api/users/kyc normalizes lowercase state to uppercase": Mock session, call KYC POST with `state: "ca"`. Mock prisma.user.upsert to capture args. Assert the upsert was called with `kycState: "CA"`.

4. "POST /api/redeem/shipping rejects restricted state": Call shipping POST with `state: "NY"` (a restricted state). Assert response status 400 with validation error about restricted state.

5. "POST /api/redeem/shipping normalizes state code": Call shipping POST with `state: "tx"` (allowed, lowercase). The Zod schema transforms to "TX". Assert validation passes (or assert prisma was called with uppercased state).

6. "POST /api/redeem/shipping rejects invalid state code": Call shipping POST with `state: "ZZ"` (invalid). Assert 400 with "Invalid US state code".

For compliance tests, mock prisma methods to return appropriate data. The redeem shipping tests need a mock order in the DB (mock `prisma.order.findUnique` to return a REDEEM order owned by the session user).
  </action>
  <verify>Run `cd /Users/chiranjibipoudyal/programming/side_projects/ammo-exchange/apps/web && npx vitest run` -- all tests pass.</verify>
  <done>Auth tests prove unauthenticated=401 and non-keeper=403. Compliance tests prove KYC masking, state normalization, and restricted state rejection.</done>
</task>

</tasks>

<verification>
1. `cd /Users/chiranjibipoudyal/programming/side_projects/ammo-exchange/apps/web && npx vitest run` -- all tests pass
2. `pnpm --filter @ammo-exchange/web check` -- typecheck passes
3. Auth test output shows 401 for unauthenticated, 403 for non-keeper
4. Compliance test output shows masked gov ID, normalized state codes, restricted state rejection
</verification>

<success_criteria>
- TEST-03 covered: Auth tests verify 401 for unauthenticated, 403 for non-keeper admin access
- TEST-04 covered: Compliance tests verify restricted-state rejection, state normalization (ca -> CA), KYC masking (****XXXX)
- All tests pass with vitest
- No regressions in web app typecheck
</success_criteria>

<output>
After completion, create `.planning/phases/31-test-suite/31-02-SUMMARY.md`
</output>
