---
phase: 30-architecture-contract-hardening
plan: 03
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - packages/contracts/src/abis/CaliberMarket.ts
  - packages/shared/src/config/index.ts
  - packages/contracts/script/DeployFuji.s.sol
autonomous: false

must_haves:
  truths:
    - "Updated CaliberMarket ABI includes DeadlineInPast, PriceTooLow, PriceTooHigh errors and maxPriceDeviationBps getter"
    - "All contract addresses in shared config point to newly deployed Fuji contracts"
    - "DEPLOYMENT_BLOCKS.fuji is updated to the new deployment block"
    - "Frontend and worker typecheck against the new ABI without errors"
  artifacts:
    - path: "packages/contracts/src/abis/CaliberMarket.ts"
      provides: "Updated ABI with new error types"
      contains: "DeadlineInPast"
    - path: "packages/shared/src/config/index.ts"
      provides: "Updated Fuji contract addresses and deployment block"
  key_links:
    - from: "packages/contracts/src/abis/CaliberMarket.ts"
      to: "packages/shared/src/config/index.ts"
      via: "ABI used with contract addresses for on-chain reads"
    - from: "packages/shared/src/config/index.ts"
      to: "apps/worker/src/lib/constants.ts"
      via: "CONTRACT_ADDRESSES import"
      pattern: "CONTRACT_ADDRESSES"
---

<objective>
Export updated ABIs, redeploy all contracts to Fuji testnet, and update shared config with new addresses.

Purpose: The contract changes from Plan 01 (deadline validation + price sanity bounds) need to be deployed on-chain and reflected in the TypeScript ABI exports so the frontend and worker see the new error types.
Output: New ABI exports, fresh Fuji deployment with new addresses, and updated DEPLOYMENT_BLOCKS.
</objective>

<execution_context>
@/Users/chiranjibipoudyal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiranjibipoudyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/contracts/scripts/export-abis.ts
@packages/contracts/Makefile
@packages/shared/src/config/index.ts
@.planning/phases/30-architecture-contract-hardening/30-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export updated ABIs from compiled contracts</name>
  <files>packages/contracts/src/abis/CaliberMarket.ts</files>
  <action>
Run the ABI export process to regenerate TypeScript ABI files from the freshly compiled contracts (which now include DeadlineInPast, PriceTooLow, PriceTooHigh errors and maxPriceDeviationBps/setMaxPriceDeviation).

Steps:

1. `cd packages/contracts && forge build` — compile contracts
2. `pnpm contracts:build` — or run `npx tsx scripts/export-abis.ts` to regenerate ABI files in `packages/contracts/src/abis/`

After export, verify that the CaliberMarket ABI file contains:

- `DeadlineInPast` error type
- `PriceTooLow` error type
- `PriceTooHigh` error type
- `maxPriceDeviationBps` function (view)
- `setMaxPriceDeviation` function

Then run typecheck to ensure the new ABI is compatible:

```bash
pnpm --filter @ammo-exchange/web check
pnpm --filter @ammo-exchange/worker check
```

If the frontend has error message parsing that maps contract errors to human-readable strings (e.g., in `parseContractError` or similar), add the three new error names to that mapping:

- `DeadlineInPast` → "The deadline has already passed"
- `PriceTooLow` → "Price is below the acceptable range"
- `PriceTooHigh` → "Price exceeds the acceptable range"
  </action>
  <verify>Verify CaliberMarket ABI TypeScript file contains DeadlineInPast, PriceTooLow, PriceTooHigh. Run `pnpm check` to confirm all packages typecheck.</verify>
  <done>Updated ABI exports contain all new error types and functions. All packages typecheck cleanly.</done>
  </task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy contracts to Fuji testnet</name>
  <action>
Claude cannot deploy contracts because it requires a private key. The user must run the deployment.

**Pre-deployment checklist (Claude completes):**

1. Verify `forge build` succeeds with no errors
2. Verify `forge test` passes all tests
3. Confirm `.env` has FUJI_RPC_URL, PRIVATE_KEY, SNOWTRACE_API_KEY

**User deploys:**

```bash
cd packages/contracts && make fuji_deploy
```

The deployment will output all new contract addresses. Record:

- MockUSDC address
- AmmoManager address
- AmmoFactory address
- Per-caliber: oracle, market, token addresses (4 calibers x 3 = 12 addresses)
- The block number of the deployment transaction
  </action>
  <how-to-verify> 1. Run `cd packages/contracts && make fuji_deploy` 2. Copy the logged addresses from the deployment output 3. Paste the addresses here so Claude can update shared config 4. Note the deployment block number from the transaction receipt
  </how-to-verify>
  <resume-signal>Paste the deployment output with all contract addresses and the deployment block number</resume-signal>
  </task>

<task type="auto">
  <name>Task 3: Update shared config with new Fuji addresses and deployment block</name>
  <files>packages/shared/src/config/index.ts</files>
  <action>
Using the addresses provided by the user from the deployment output:

1. Update `CONTRACT_ADDRESSES.fuji` with all new addresses:
   - `manager`: new AmmoManager address
   - `factory`: new AmmoFactory address
   - `usdc`: new MockUSDC address
   - `calibers.9MM.market` and `calibers.9MM.token`: new 9MM addresses
   - `calibers.556.market` and `calibers.556.token`: new 556 addresses
   - `calibers.22LR.market` and `calibers.22LR.token`: new 22LR addresses
   - `calibers.308.market` and `calibers.308.token`: new 308 addresses

2. Update `DEPLOYMENT_BLOCKS.fuji` to the new deployment block number (use `BigInt(BLOCK_NUMBER)`)

3. Run typecheck to confirm:

```bash
pnpm --filter @ammo-exchange/shared check
pnpm --filter @ammo-exchange/web check
pnpm --filter @ammo-exchange/worker check
```

All address values must be checksummed `0x${string}` format as shown in the deployment output.
</action>
<verify>Run `pnpm check` to confirm all packages typecheck with the new addresses.</verify>
<done>All Fuji contract addresses and deployment block updated in shared config. All packages typecheck cleanly.</done>
</task>

</tasks>

<verification>
```bash
pnpm check
```
All packages typecheck. New ABI includes DeadlineInPast, PriceTooLow, PriceTooHigh. Shared config has new Fuji addresses and deployment block.
</verification>

<success_criteria>

- CaliberMarket ABI TypeScript file includes all new error types and functions
- Frontend error parser handles the three new contract errors
- All Fuji contract addresses are updated to fresh deployment
- DEPLOYMENT_BLOCKS.fuji points to the new deployment block
- Full monorepo typecheck passes
  </success_criteria>

<output>
After completion, create `.planning/phases/30-architecture-contract-hardening/30-03-SUMMARY.md`
</output>
