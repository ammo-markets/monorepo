---
status: complete
phase: 09.1-admin-kyc-profile
source: [09.1-01-SUMMARY.md, 09.1-02-SUMMARY.md, 09.1-03-SUMMARY.md, 09.1-04-SUMMARY.md]
started: 2026-02-15T13:15:00Z
updated: 2026-02-15T14:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Admin 404 for non-keeper
expected: Navigate to /admin while not signed in (or signed in with a non-keeper wallet). You see a 404 page immediately — no admin HTML content flashes before the 404.
result: pass

### 2. Admin accessible for keeper
expected: Sign in with a keeper wallet. Navigate to /admin. The admin dashboard loads normally with sidebar and content area.
result: pass

### 3. Admin link visible only for keepers
expected: Sign in with a keeper wallet. The navbar shows an "Admin" link. Sign out or switch to a non-keeper wallet — the "Admin" link disappears from the navbar.
result: pass

### 4. Profile link visible when authenticated
expected: Sign in with SIWE (any wallet). The navbar shows a "Profile" link. Sign out — the "Profile" link disappears.
result: issue
reported: "Profile link is actually visible, but when I click, and when I'm actually logged in, I see an empty screen."
severity: major
fix: "Replaced 100ms timer auth guard with isSessionLoading flag in useSiwe hook (commit 8a47880)"

### 5. KYC form in redeem flow
expected: Navigate to Redeem with a wallet that has KYC status NONE. Step 2 (KYC) shows a form with 5 fields: Full Legal Name, Date of Birth, State of Residence, Government ID Type, Government ID Number. The old "Verify My Identity" button is gone.
result: issue
reported: "Prisma error: Unknown field defaultShippingName — stale Prisma client after migration"
severity: major
fix: "Stale Prisma client in dev server — resolved by pnpm db:generate + dev server restart"

### 6. KYC form validation
expected: On the KYC form, try submitting with empty fields — validation errors appear. Enter a date of birth that makes you under 18 — age validation error shows. Enter a gov ID number less than 5 characters — error shows. Fill all fields correctly and submit — status changes to APPROVED.
result: pass

### 7. Profile page wallet section
expected: Navigate to /profile while signed in. You see a "Wallet" card showing your full wallet address in monospace font, with a copy button. Click copy — address copies to clipboard (icon changes to checkmark briefly).
result: pass

### 8. Profile page KYC status badge
expected: On the /profile page, the "Identity Verification" card shows a color-coded badge: green "Verified" if APPROVED, amber "Pending" if PENDING, red "Rejected" if REJECTED, gray "Not Verified" if NONE. If not approved, a "Complete verification" link appears.
result: pass

### 9. Profile page shipping address
expected: On the /profile page, the "Default Shipping Address" section shows "No default address saved" with an "Add Address" button. Click it — an inline form appears with Name, Line 1, Line 2, City, State (dropdown), ZIP fields. Fill and save — the address displays formatted. Click "Edit" — the form reappears pre-populated. Edit and save — changes persist on page reload.
result: pass

### 10. Profile page auth guard
expected: Open /profile in a browser where you are NOT signed in with SIWE. You get redirected to the home page (not a blank or error page).
result: issue
reported: "pass but there is a problem with profile page the navigation header is not available in that page"
severity: major
fix: "Added Navbar and Footer to profile page matching all other pages (commit 95a6b07)"

## Summary

total: 10
passed: 7
issues: 3
pending: 0
skipped: 0

## Gaps

[all issues fixed during testing — no remaining gaps]
