---
phase: 13-app-shell-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/(landing)/layout.tsx
  - apps/web/app/(landing)/page.tsx
  - apps/web/app/(app)/layout.tsx
  - apps/web/app/(app)/dashboard/page.tsx
  - apps/web/app/(app)/trade/page.tsx
  - apps/web/app/(app)/portfolio/page.tsx
  - apps/web/app/(app)/portfolio/orders/[id]/page.tsx
  - apps/web/app/(app)/profile/page.tsx
  - apps/web/app/(app)/market/page.tsx
  - apps/web/app/(app)/market/[caliber]/page.tsx
  - apps/web/app/(app)/mint/page.tsx
  - apps/web/app/(app)/redeem/page.tsx
autonomous: true

must_haves:
  truths:
    - "Landing page renders at / with Navbar, Hero, HowItWorks, MarketCards, ProtocolStats, Footer"
    - "App routes (/dashboard, /trade, /portfolio, /profile) render inside (app) route group layout"
    - "Visiting /dashboard without wallet connected redirects to /"
    - "Admin routes remain functional at /admin with existing keeper protection"
    - "All existing page components render without errors in their new route group locations"
  artifacts:
    - path: "apps/web/app/(landing)/layout.tsx"
      provides: "Public layout with Navbar + Footer wrapper"
    - path: "apps/web/app/(landing)/page.tsx"
      provides: "Landing page (moved from app/page.tsx)"
    - path: "apps/web/app/(app)/layout.tsx"
      provides: "Wallet-gated app layout with redirect to / if not connected"
    - path: "apps/web/app/(app)/dashboard/page.tsx"
      provides: "Dashboard placeholder page"
  key_links:
    - from: "apps/web/app/(app)/layout.tsx"
      to: "hooks/use-wallet.ts"
      via: "useWallet().isConnected check + redirect"
      pattern: "useWallet|isConnected|redirect|router.push"
    - from: "apps/web/app/(landing)/layout.tsx"
      to: "features/layout/navbar.tsx"
      via: "Navbar + Footer import"
      pattern: "import.*Navbar|import.*Footer"
---

<objective>
Split the Next.js app into two route groups: `(landing)` for public pages and `(app)` for wallet-connected pages. Move all existing routes into the appropriate group and add a wallet connection gate on the app group.

Purpose: SHELL-01 (route group split) and SHELL-03 (wallet gate redirect) — foundation for the 4-tab navigation added in plan 02.
Output: Two route groups with distinct layouts, wallet gate on app routes, all existing pages functional in new locations.
</objective>

<execution_context>
@/Users/chiranjibipoudyal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiranjibipoudyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/app/layout.tsx
@apps/web/app/page.tsx
@apps/web/app/providers.tsx
@apps/web/features/layout/navbar.tsx
@apps/web/hooks/use-wallet.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create route groups and move existing routes</name>
  <files>
    apps/web/app/(landing)/layout.tsx
    apps/web/app/(landing)/page.tsx
    apps/web/app/(app)/layout.tsx
    apps/web/app/(app)/dashboard/page.tsx
    apps/web/app/(app)/trade/page.tsx
    apps/web/app/(app)/portfolio/page.tsx
    apps/web/app/(app)/portfolio/orders/[id]/page.tsx
    apps/web/app/(app)/profile/page.tsx
    apps/web/app/(app)/market/page.tsx
    apps/web/app/(app)/market/[caliber]/page.tsx
    apps/web/app/(app)/mint/page.tsx
    apps/web/app/(app)/redeem/page.tsx
  </files>
  <action>
    Create two Next.js route groups under `apps/web/app/`:

    1. **`(landing)/` route group** — public pages:
       - `(landing)/layout.tsx`: Server component wrapping children with Navbar (top) + Footer (bottom) in a flex column, min-h-screen. Import from `@/features/layout`. This layout provides the chrome that was previously inline in `app/page.tsx`.
       - `(landing)/page.tsx`: Move current `app/page.tsx` content here, but REMOVE the Navbar/Footer since the layout provides them. Keep Hero, HowItWorks, MarketTicker, MarketCards, ProtocolStats inside a `<main className="flex-1">`.

    2. **`(app)/` route group** — wallet-connected pages:
       - `(app)/layout.tsx`: Client component ("use client"). Uses `useWallet()` from `@/hooks/use-wallet` and `useRouter()` from `next/navigation`. On mount (useEffect), if `!isConnected && !isReconnecting`, redirect to `/` via `router.replace("/")`. While checking or redirecting, show a centered loading spinner or null. When connected, render `{children}` wrapped in a flex layout (the nav will be added in plan 02 — for now just render children in a div with min-h-screen). Do NOT include Navbar/Footer here — plan 02 adds the app nav.
       - `(app)/dashboard/page.tsx`: Create a minimal placeholder page: `<div className="p-8"><h1 className="text-2xl font-bold" style={{color: "var(--text-primary)"}}>Dashboard</h1><p style={{color: "var(--text-secondary)"}}>Coming soon in Phase 14.</p></div>`. This ensures /dashboard route works immediately.

    3. **Move existing routes** into `(app)/` route group by moving directories:
       - `app/trade/` → `app/(app)/trade/` (move page.tsx and trade-demo.tsx)
       - `app/portfolio/` → `app/(app)/portfolio/` (move page.tsx, orders/[id]/page.tsx, error.tsx)
       - `app/profile/` → `app/(app)/profile/` (move page.tsx, error.tsx)
       - `app/market/` → `app/(app)/market/` (move page.tsx, [caliber]/page.tsx, error.tsx)
       - `app/mint/` → `app/(app)/mint/` (move page.tsx, error.tsx)
       - `app/redeem/` → `app/(app)/redeem/` (move page.tsx, error.tsx)

    4. **Keep untouched:**
       - `app/layout.tsx` (root layout — stays as-is with fonts, Providers, Toaster)
       - `app/error.tsx` (root error boundary)
       - `app/globals.css`
       - `app/providers.tsx`
       - `app/admin/` (admin routes stay at top level with existing keeper-gated layout)
       - `app/api/` (API routes stay at top level)

    5. **Delete old files** after moving:
       - Remove `app/page.tsx` (moved to `(landing)/page.tsx`)
       - Remove old `app/trade/`, `app/portfolio/`, `app/profile/`, `app/market/`, `app/mint/`, `app/redeem/` directories

    IMPORTANT: The (app) layout should handle the wallet reconnection grace period. Use `useAccount` status: if status is "reconnecting", show a loading state (don't redirect). Only redirect when status is "disconnected" (not reconnecting). This prevents flash-redirect on page refresh when wagmi is re-establishing the connection.

  </action>
  <verify>
    Run `pnpm --filter @ammo-exchange/web check` to verify TypeScript compilation.
    Run `pnpm --filter @ammo-exchange/web build` to verify the build succeeds.
    Verify route structure: `find apps/web/app -name "page.tsx" | sort` should show pages under (landing)/ and (app)/ groups, plus admin/.
  </verify>
  <done>
    - `/` renders landing page with Hero, HowItWorks, MarketCards, ProtocolStats inside (landing) layout with Navbar + Footer
    - `/dashboard` renders placeholder page inside (app) layout
    - `/trade`, `/portfolio`, `/profile`, `/market`, `/mint`, `/redeem` render under (app) route group
    - `/admin` routes untouched and still functional
    - Navigating to any (app) route without connected wallet redirects to `/`
    - TypeScript compilation passes
    - Build succeeds
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @ammo-exchange/web check` passes
2. `pnpm --filter @ammo-exchange/web build` succeeds
3. Route structure shows (landing) and (app) groups: `find apps/web/app -name "page.tsx" -not -path "*/node_modules/*" | sort`
4. No orphaned pages remain in old locations (app/trade/, app/portfolio/, etc.)
</verification>

<success_criteria>

- Landing page at `/` renders with Navbar + Footer from (landing) layout
- All app routes live under `(app)/` route group with wallet gate
- `/dashboard` route exists with placeholder content
- Admin routes unchanged at `/admin`
- API routes unchanged at `/api`
- TypeScript check and build pass
  </success_criteria>

<output>
After completion, create `.planning/phases/13-app-shell-restructure/13-01-SUMMARY.md`
</output>
