---
phase: 13-app-shell-restructure
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - apps/web/features/layout/app-nav.tsx
  - apps/web/features/layout/index.ts
  - apps/web/app/(app)/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Desktop shows sidebar navigation on the left with 4 tabs (Dashboard, Trade, Portfolio, Profile) and content on the right"
    - "Mobile shows bottom tab bar with 4 tabs (Dashboard, Trade, Portfolio, Profile)"
    - "Active tab is visually highlighted based on current pathname"
    - "Sidebar shows logo at top and wallet button, network badge, and admin link (if keeper) in sidebar"
    - "Navigation is responsive — sidebar hidden below lg breakpoint, bottom tabs hidden at lg and above"
  artifacts:
    - path: "apps/web/features/layout/app-nav.tsx"
      provides: "Responsive navigation component with sidebar (desktop) and bottom tabs (mobile)"
    - path: "apps/web/app/(app)/layout.tsx"
      provides: "App layout integrating AppNav with wallet gate"
  key_links:
    - from: "apps/web/features/layout/app-nav.tsx"
      to: "next/navigation"
      via: "usePathname for active state detection"
      pattern: "usePathname|pathname"
    - from: "apps/web/app/(app)/layout.tsx"
      to: "apps/web/features/layout/app-nav.tsx"
      via: "AppNav import in layout"
      pattern: "import.*AppNav|AppSidebar|AppBottomTabs"
---

<objective>
Build the responsive 4-tab navigation for the app route group: sidebar on desktop (lg+), bottom tabs on mobile (<lg). Wire it into the (app) layout created in plan 01.

Purpose: SHELL-02 (4-tab navigation with active states) and SHELL-04 (responsive sidebar/bottom tabs).
Output: Fully functional responsive navigation with Dashboard, Trade, Portfolio, Profile tabs.
</objective>

<execution_context>
@/Users/chiranjibipoudyal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiranjibipoudyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-app-shell-restructure/13-01-SUMMARY.md
@apps/web/features/layout/navbar.tsx
@apps/web/features/layout/logo.tsx
@apps/web/features/layout/wallet-button.tsx
@apps/web/hooks/use-wallet.ts
@apps/web/hooks/use-keeper-check.ts
@apps/web/hooks/use-siwe.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AppNav component with sidebar and bottom tabs</name>
  <files>
    apps/web/features/layout/app-nav.tsx
    apps/web/features/layout/index.ts
  </files>
  <action>
    Create `apps/web/features/layout/app-nav.tsx` — a "use client" component providing:

    **Navigation items** (constant array):
    - Dashboard: icon = LayoutDashboard (lucide-react), href = "/dashboard"
    - Trade: icon = ArrowLeftRight (lucide-react), href = "/trade"
    - Portfolio: icon = Wallet (lucide-react), href = "/portfolio"
    - Profile: icon = User (lucide-react), href = "/profile"

    **Active state detection:** Use `usePathname()` from `next/navigation`. A link is active if `pathname === href` or `pathname.startsWith(href + "/")` (for nested routes like /portfolio/orders/123). Exception: "/" should only match exactly.

    **Desktop sidebar** (visible at `lg:` breakpoint, hidden below):
    - Fixed position on left, full viewport height, width ~64px collapsed or ~240px expanded (use 240px fixed width for simplicity).
    - Background: `var(--bg-secondary)`, right border: `var(--border-default)`.
    - Top section: AmmoLogo component (import from `./logo`).
    - Middle section: Nav links as vertical list. Each link: icon (20px) + label text. Active link has `var(--accent)` color and a left border accent or background highlight (`var(--bg-tertiary)`). Inactive links: `var(--text-secondary)` color, hover → `var(--text-primary)`.
    - Bottom section: Network badge (Avalanche Fuji with dot indicator, same as existing Navbar), WalletButton component. If `isKeeper` (from useKeeperCheck), show an "Admin" link to `/admin`. If `isSignedIn` show additional context.
    - Use `<Link>` from `next/link` for all navigation (not `<a>` tags) for client-side navigation.

    **Mobile bottom tabs** (visible below `lg:` breakpoint, hidden at lg+):
    - Fixed position at bottom of viewport, full width, height ~64px.
    - Background: `var(--bg-secondary)`, top border: `var(--border-default)`.
    - 4 tabs in a horizontal row (flex, justify-evenly or grid-cols-4).
    - Each tab: icon (20px) centered above label text (text-xs). Active tab: `var(--accent)` color. Inactive: `var(--text-muted)`.
    - Use `<Link>` from `next/link`.
    - Add safe area padding for notched devices: `pb-[env(safe-area-inset-bottom)]`.

    **Export pattern:** Export a single `AppNav` component that renders BOTH the sidebar (hidden on mobile) and bottom tabs (hidden on desktop). This keeps the layout simple — one import, one component.

    Update `apps/web/features/layout/index.ts` to export `AppNav`.

  </action>
  <verify>
    Run `pnpm --filter @ammo-exchange/web check` to verify TypeScript compilation passes.
  </verify>
  <done>
    - AppNav component exists with sidebar (desktop) and bottom tabs (mobile)
    - 4 navigation tabs: Dashboard, Trade, Portfolio, Profile
    - Active state detection works via usePathname
    - Sidebar shows logo, nav links, network badge, wallet button, optional admin link
    - Bottom tabs show icon + label with active highlighting
    - Component exported from features/layout/index.ts
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire AppNav into (app) layout</name>
  <files>
    apps/web/app/(app)/layout.tsx
  </files>
  <action>
    Update the `(app)/layout.tsx` created in plan 01 to integrate the AppNav component:

    Layout structure:
    ```
    <div className="flex min-h-screen">
      <AppNav />
      <main className="flex-1 lg:ml-[240px] pb-16 lg:pb-0">
        {children}
      </main>
    </div>
    ```

    Key details:
    - Import `AppNav` from `@/features/layout`.
    - The main content area uses `lg:ml-[240px]` to offset for the fixed sidebar on desktop.
    - The main content area uses `pb-16 lg:pb-0` to add bottom padding on mobile (for the fixed bottom tabs) and remove it on desktop.
    - Keep the existing wallet gate logic (redirect to / if not connected, loading state during reconnection).
    - The AppNav renders after the wallet connection check passes (inside the connected state render).
    - Ensure the layout does NOT include Navbar or Footer — the AppNav replaces them for app routes.

  </action>
  <verify>
    Run `pnpm --filter @ammo-exchange/web check` to verify TypeScript compilation.
    Run `pnpm --filter @ammo-exchange/web build` to verify build succeeds.
  </verify>
  <done>
    - (app) layout renders AppNav sidebar on desktop with content offset
    - (app) layout renders AppNav bottom tabs on mobile with content bottom padding
    - Wallet gate still works (redirect to / if disconnected)
    - Build passes successfully
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @ammo-exchange/web check` passes
2. `pnpm --filter @ammo-exchange/web build` succeeds
3. AppNav exports from features/layout/index.ts
4. (app)/layout.tsx imports and renders AppNav
</verification>

<success_criteria>

- Desktop: sidebar nav on left (240px wide) with logo, 4 tabs, wallet button, network badge
- Mobile: bottom tab bar with 4 icon+label tabs
- Active tab highlighted with accent color based on current pathname
- Content area properly offset (ml on desktop, pb on mobile)
- Wallet gate still functional
- TypeScript check and build pass
  </success_criteria>

<output>
After completion, create `.planning/phases/13-app-shell-restructure/13-02-SUMMARY.md`
</output>
