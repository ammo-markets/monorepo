---
phase: 11-frontend-data-layer-and-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/providers.tsx
  - apps/web/hooks/use-market-data.ts
  - apps/web/hooks/use-orders.ts
  - apps/web/hooks/use-admin-stats.ts
  - apps/web/hooks/use-activity.ts
  - apps/web/features/market/market-cards.tsx
  - apps/web/features/market/market-table.tsx
  - apps/web/features/market/market-ticker.tsx
  - apps/web/features/market/activity-feed.tsx
  - apps/web/features/home/protocol-stats.tsx
  - apps/web/features/admin/protocol-stats.tsx
  - apps/web/features/portfolio/portfolio-dashboard.tsx
  - apps/web/features/portfolio/order-detail.tsx
  - apps/web/app/market/[caliber]/page.tsx
  - apps/web/features/mint/mint-flow.tsx
  - apps/web/features/redeem/redeem-flow.tsx
  - apps/web/features/trade/swap-widget.tsx
  - apps/web/app/profile/page.tsx
  - apps/web/features/admin/finalize-mint-dialog.tsx
  - apps/web/features/admin/finalize-redeem-dialog.tsx
autonomous: true

must_haves:
  truths:
    - "No component uses raw useEffect+fetch -- all data loading goes through TanStack Query hooks"
    - "Loading and error states are handled via useQuery's isLoading/error, not manual useState"
    - "Admin finalize actions trigger cache invalidation so order tables update without refresh"
    - "QueryClient is configured with sensible retry and refetch defaults"
  artifacts:
    - path: "apps/web/app/providers.tsx"
      provides: "QueryClient with retry/refetch configuration"
      contains: "staleTime"
    - path: "apps/web/hooks/use-market-data.ts"
      provides: "Shared useMarketData hook wrapping useQuery"
      exports: ["useMarketData"]
    - path: "apps/web/hooks/use-orders.ts"
      provides: "useOrders and useOrderDetail hooks"
      exports: ["useOrders", "useOrderDetail"]
    - path: "apps/web/hooks/use-admin-stats.ts"
      provides: "useAdminStats hook"
      exports: ["useAdminStats"]
    - path: "apps/web/hooks/use-activity.ts"
      provides: "useActivity hook"
      exports: ["useActivity"]
  key_links:
    - from: "apps/web/features/admin/finalize-mint-dialog.tsx"
      to: "queryClient"
      via: "useQueryClient().invalidateQueries on finalize success"
      pattern: "invalidateQueries.*admin.*orders"
    - from: "apps/web/features/admin/finalize-redeem-dialog.tsx"
      to: "queryClient"
      via: "useQueryClient().invalidateQueries on finalize success"
      pattern: "invalidateQueries.*admin.*orders"
    - from: "apps/web/features/market/market-cards.tsx"
      to: "apps/web/hooks/use-market-data.ts"
      via: "useMarketData() hook call"
      pattern: "useMarketData"
---

<objective>
Replace all useEffect+fetch patterns with TanStack Query hooks, configure QueryClient with retry/refetch defaults, and wire cache invalidation on admin finalize actions.

Purpose: Eliminate silent error swallowing, enable automatic retries, and ensure admin actions immediately reflect in the UI.
Output: Custom query hooks (use-market-data, use-orders, use-admin-stats, use-activity), updated QueryClient config, and all 13+ components migrated.
</objective>

<execution_context>
@/Users/chiranjibipoudyal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiranjibipoudyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TanStack Query hooks and configure QueryClient</name>
  <files>
    apps/web/app/providers.tsx
    apps/web/hooks/use-market-data.ts
    apps/web/hooks/use-orders.ts
    apps/web/hooks/use-admin-stats.ts
    apps/web/hooks/use-activity.ts
  </files>
  <action>
1. **Update `apps/web/app/providers.tsx`**: Configure QueryClient with defaults:
   - `staleTime: 30_000` (30s -- data doesn't go stale instantly)
   - `retry: 2` (retry twice on failure)
   - `refetchOnWindowFocus: true` (default, but be explicit)
   - Wrap QueryClient creation in a `useState` or `useRef` to avoid recreation on re-renders (current code creates `new QueryClient()` at module level which is fine for SSR-safe patterns but inconsistent with React best practice -- keep module-level since it works and Next.js 15 App Router does this)
   - Actually, keep the module-level `const queryClient = new QueryClient({...})` since it already works this way

2. **Create `apps/web/hooks/use-market-data.ts`**: Shared hook that wraps `useQuery` for `/api/market`:
   ```ts
   import { useQuery } from "@tanstack/react-query";
   import type { MarketCaliberFromAPI } from "@/lib/types";

   interface MarketResponse { calibers: MarketCaliberFromAPI[] }

   export function useMarketData() {
     return useQuery<MarketCaliberFromAPI[]>({
       queryKey: ["market"],
       queryFn: async () => {
         const res = await fetch("/api/market");
         if (!res.ok) throw new Error("Failed to fetch market data");
         const data = (await res.json()) as MarketResponse;
         return data.calibers ?? [];
       },
       staleTime: 60_000, // market data: 1 minute stale
     });
   }
   ```

3. **Create `apps/web/hooks/use-orders.ts`**: Two hooks:
   - `useOrders()`: fetches `/api/orders`, returns orders array. Use `enabled: !!address` pattern by accepting an address parameter.
   - `useOrderDetail(orderId: string)`: fetches `/api/orders/{id}`

4. **Create `apps/web/hooks/use-admin-stats.ts`**: Hook wrapping `useQuery` for `/api/admin/stats`. Return the StatsData type (define inline or import).

5. **Create `apps/web/hooks/use-activity.ts`**: Hook wrapping `useQuery` for `/api/activity`.

All hooks must use `import type` for type-only imports per project conventions. All files must be ESM.
  </action>
  <verify>
Run `pnpm --filter @ammo-exchange/web check` -- should compile without errors (new hooks exist but aren't consumed yet, which is fine for types).
  </verify>
  <done>
Five query hooks exist (use-market-data, use-orders with useOrders+useOrderDetail, use-admin-stats, use-activity). QueryClient has staleTime and retry defaults. All hooks export typed return values.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate all components from useEffect+fetch to query hooks and wire cache invalidation</name>
  <files>
    apps/web/features/market/market-cards.tsx
    apps/web/features/market/market-table.tsx
    apps/web/features/market/market-ticker.tsx
    apps/web/features/market/activity-feed.tsx
    apps/web/features/home/protocol-stats.tsx
    apps/web/features/admin/protocol-stats.tsx
    apps/web/features/portfolio/portfolio-dashboard.tsx
    apps/web/features/portfolio/order-detail.tsx
    apps/web/app/market/[caliber]/page.tsx
    apps/web/features/mint/mint-flow.tsx
    apps/web/features/redeem/redeem-flow.tsx
    apps/web/features/trade/swap-widget.tsx
    apps/web/app/profile/page.tsx
    apps/web/features/admin/finalize-mint-dialog.tsx
    apps/web/features/admin/finalize-redeem-dialog.tsx
  </files>
  <action>
For EACH component listed below, replace the `useEffect+fetch+useState` pattern with the corresponding query hook from Task 1. Preserve all existing UI rendering logic, skeleton states, and error states. Map `isLoading` from useQuery to the existing loading UI, and `error` to the existing error UI (or add error UI if the component was silently swallowing errors with `.catch(() => {})`).

**Market components (all use `useMarketData()`):**
- `market-cards.tsx`: Replace `useState+useEffect` block (lines ~132-142) with `const { data: calibers = [], isLoading: loading } = useMarketData();`. Remove `useState` for calibers/loading. Remove `useEffect` import if no longer needed.
- `market-table.tsx`: Same pattern -- replace lines ~266-275. Keep sort state as local `useState`.
- `market-ticker.tsx`: Replace lines ~8-18 with `useMarketData()`. Map `isLoading` to existing `loading` variable.
- `activity-feed.tsx`: Replace with `useActivity()`. The component silently swallows errors -- add a simple error fallback: show "Failed to load activity" text.

**Home page:**
- `home/protocol-stats.tsx`: Replace `useEffect` (lines ~97-115) with `useMarketData()`. Derive TVL and rounds from `data` in a `useMemo`.

**Admin:**
- `admin/protocol-stats.tsx`: Replace with `useAdminStats()`. The component already has error/loading/retry UI -- wire `refetch` from the hook to the refresh button. Remove the manual `fetchStats` function.

**Portfolio:**
- `portfolio-dashboard.tsx`: Replace TWO useEffect+fetch blocks:
  1. Market data (lines ~893-900): use `useMarketData()`
  2. Orders (lines ~903-918): use `useOrders(address)` -- pass address, hook handles `enabled`
  Remove `useState` for `marketData/marketLoading/orders/ordersLoading`. Wire `isLoading` from both hooks.
- `order-detail.tsx`: Replace `useEffect` (lines ~508-519) with `useOrderDetail(orderId)`. Wire loading/error states.

**Caliber detail page:**
- `app/market/[caliber]/page.tsx`: Replace `useEffect+fetch` for market data with `useMarketData()`. Find the specific caliber from the array.

**Mint and Redeem flows:**
- `mint-flow.tsx`: Find the `useEffect` that fetches `/api/market` and replace with `useMarketData()`. These are large components -- only change the data fetching, preserve everything else.
- `redeem-flow.tsx`: Same as mint-flow.

**Trade:**
- `swap-widget.tsx`: Replace the `useEffect` that fetches `/api/market` with `useMarketData()`.

**Profile:**
- `app/profile/page.tsx`: This fetches `/api/users/profile`. Create an inline `useQuery` call (not a separate hook since it's only used here): `useQuery({ queryKey: ["profile"], queryFn: ... })`. Replace the `useEffect+fetch+useState` pattern.

**Cache invalidation (DATA-03):**
- `finalize-mint-dialog.tsx`: Import `useQueryClient` from `@tanstack/react-query`. In the `useEffect` that fires on `isConfirmed`, add `queryClient.invalidateQueries({ queryKey: ["admin", "orders"] })` to force the mint orders table to refetch. This replaces the manual `refetch()` call in the parent.
- `finalize-redeem-dialog.tsx`: Same pattern -- invalidate `["admin", "orders"]` on successful finalize.

**IMPORTANT:** For every component, ensure:
- `import React from "react"` is NOT added (React 19 doesn't need it)
- Remove unused `useState`/`useEffect` imports after migration
- Keep `"use client"` directive at top
- Use `import type` for type-only imports
  </action>
  <verify>
1. Run `pnpm --filter @ammo-exchange/web check` -- zero type errors
2. Run `grep -r "useEffect.*fetch\|\.then.*setState\|\.catch.*{}" apps/web/features apps/web/app --include="*.tsx" --include="*.ts" | grep -v node_modules | grep -v ".next"` -- should return zero results (no more useEffect+fetch patterns)
  </verify>
  <done>
All 13+ components use TanStack Query hooks instead of useEffect+fetch. No `.catch(() => {})` silent error swallowing remains. Admin finalize dialogs invalidate query cache on success. `pnpm check` passes with zero errors.
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @ammo-exchange/web check` passes
2. `grep -rn "useEffect" apps/web/features apps/web/app --include="*.tsx" | grep -v "node_modules" | grep -v ".next"` -- remaining useEffects should only be for non-data-fetching purposes (e.g., intersection observers, transaction confirmation reactions in finalize dialogs, wallet address changes in use-siwe)
3. `grep -rn "\.catch(() => {})" apps/web/ --include="*.tsx" --include="*.ts" | grep -v node_modules | grep -v ".next"` returns zero results
4. Admin mint/redeem order tables use `useQuery` with `queryKey: ["admin", "orders", ...]` and finalize dialogs call `invalidateQueries`
</verification>

<success_criteria>
- Zero components use raw useEffect+fetch for data loading (DATA-01)
- Silent error swallowing replaced with query error states (DATA-02)
- Admin mutations invalidate query caches (DATA-03)
- QueryClient configured with retry and staleTime (DATA-04)
</success_criteria>

<output>
After completion, create `.planning/phases/11-frontend-data-layer-and-quality/11-01-SUMMARY.md`
</output>
