---
phase: 05-portfolio-and-data-integration
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/web/app/api/activity/route.ts
  - apps/web/features/market/market-ticker.tsx
  - apps/web/features/market/market-cards.tsx
  - apps/web/features/market/market-table.tsx
  - apps/web/features/market/caliber-header.tsx
  - apps/web/features/market/action-panel.tsx
  - apps/web/features/market/token-stats.tsx
  - apps/web/features/market/price-chart.tsx
  - apps/web/features/market/activity-feed.tsx
  - apps/web/features/trade/swap-widget.tsx
  - apps/web/features/home/protocol-stats.tsx
  - apps/web/features/mint/mint-flow.tsx
  - apps/web/features/redeem/redeem-flow.tsx
  - apps/web/app/market/[caliber]/page.tsx
  - apps/web/lib/mock-data.ts
autonomous: true

must_haves:
  truths:
    - "Market ticker shows real oracle prices from /api/market, not hardcoded values"
    - "Market table shows real oracle prices and on-chain totalSupply from /api/market"
    - "Protocol stats show real on-chain totalSupply and DB order counts, not hardcoded strings"
    - "Activity feed shows real recent orders from database via /api/activity route"
    - "Caliber detail page (/market/[caliber]) loads data from /api/market, not from caliberDetails mock object"
    - "Swap widget uses /api/market prices, not caliberDetails mock"
    - "mock-data.ts file is deleted from the codebase"
    - "No file in the codebase imports from @/lib/mock-data"
    - "pnpm build succeeds with zero errors"
  artifacts:
    - path: "apps/web/app/api/activity/route.ts"
      provides: "Recent orders endpoint for activity feed"
      exports: ["GET"]
    - path: "apps/web/features/market/market-ticker.tsx"
      provides: "Market ticker with real prices"
    - path: "apps/web/features/home/protocol-stats.tsx"
      provides: "Protocol stats from on-chain + DB data"
  key_links:
    - from: "apps/web/features/market/market-ticker.tsx"
      to: "/api/market"
      via: "fetch or props from parent"
      pattern: "api/market|pricePerRound"
    - from: "apps/web/features/market/activity-feed.tsx"
      to: "/api/activity"
      via: "fetch call"
      pattern: "api/activity"
    - from: "apps/web/features/home/protocol-stats.tsx"
      to: "on-chain totalSupply + DB counts"
      via: "fetch or direct reads"
      pattern: "totalSupply|api/"
    - from: "apps/web/app/market/[caliber]/page.tsx"
      to: "/api/market"
      via: "fetch for caliber data"
      pattern: "api/market"
---

<objective>
Replace all remaining mock-data imports across the entire codebase with real data sources and delete mock-data.ts.

Purpose: Satisfies DB-04 (no mock data remains) and completes the Phase 5 goal. After Plan 01 handles the portfolio/order-detail pages, this plan sweeps through the remaining 13 files that still import from mock-data.ts: market components, home page, trade widget, and residual imports in mint/redeem flows.

Output: Zero imports from mock-data.ts anywhere in the codebase. mock-data.ts deleted. All data displays read from /api/market, /api/activity, on-chain totalSupply, or CALIBER_SPECS. pnpm build succeeds.
</objective>

<execution_context>
@/Users/chiranjibipoudyal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiranjibipoudyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-portfolio-and-data-integration/05-RESEARCH.md
@.planning/phases/05-portfolio-and-data-integration/05-01-SUMMARY.md

# Types created in Plan 01

@apps/web/lib/types.ts

# Existing API routes

@apps/web/app/api/market/route.ts
@apps/web/app/api/orders/route.ts
@apps/web/lib/viem.ts

# Files to modify (all still import mock-data)

@apps/web/features/market/market-ticker.tsx
@apps/web/features/market/market-cards.tsx
@apps/web/features/market/market-table.tsx
@apps/web/features/market/caliber-header.tsx
@apps/web/features/market/action-panel.tsx
@apps/web/features/market/token-stats.tsx
@apps/web/features/market/price-chart.tsx
@apps/web/features/market/activity-feed.tsx
@apps/web/features/trade/swap-widget.tsx
@apps/web/features/home/protocol-stats.tsx
@apps/web/features/mint/mint-flow.tsx
@apps/web/features/redeem/redeem-flow.tsx
@apps/web/app/market/[caliber]/page.tsx
@apps/web/lib/mock-data.ts

# Shared constants

@packages/shared/src/constants/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /api/activity route and rewire market/home/trade components</name>
  <files>
    apps/web/app/api/activity/route.ts
    apps/web/features/market/market-ticker.tsx
    apps/web/features/market/market-cards.tsx
    apps/web/features/market/market-table.tsx
    apps/web/features/market/caliber-header.tsx
    apps/web/features/market/action-panel.tsx
    apps/web/features/market/token-stats.tsx
    apps/web/features/market/price-chart.tsx
    apps/web/features/market/activity-feed.tsx
    apps/web/features/trade/swap-widget.tsx
    apps/web/features/home/protocol-stats.tsx
    apps/web/app/market/[caliber]/page.tsx
  </files>
  <action>
**Create /api/activity route** (`apps/web/app/api/activity/route.ts`):
- Query the 10 most recent COMPLETED orders from Prisma, ordered by updatedAt desc
- Return: `{ activity: [{ id, type, amount, walletAddress, caliber, updatedAt }] }`
- Use `serializeBigInts` for BigInt amount field
- Use `PRISMA_TO_CALIBER` for caliber mapping
- Truncate wallet addresses server-side is optional (client can do it)
- Follow established API route pattern: Prisma query -> serialize -> Response.json

**Market ticker** (`market-ticker.tsx`):

- Replace `calibers` import from mock-data with data prop or fetch from `/api/market`
- The component likely receives caliber data as props from a parent page -- check the actual usage pattern
- If it fetches directly: use useEffect + fetch("/api/market") and map response to display format
- Drop `change24h`, `volume24h`, `sparklineData` -- not available from API (show price only, or use placeholder dashes)
- Use `CALIBER_SPECS[caliber].name` for display name

**Market cards** (`market-cards.tsx`):

- Same pattern as ticker: replace `calibers` import with /api/market data
- Drop unavailable fields (change24h, volume24h, sparklineData)

**Market table** (`market-table.tsx`):

- Replace `marketCalibers` import with /api/market data
- For totalSupply: either add totalSupply to /api/market response OR fetch separately. Simplest: read totalSupply for each AmmoToken in the market API route and include it (this means updating `/api/market/route.ts` to also read totalSupply -- do this). Add to the market route response:
  ```typescript
  // After reading prices, also read totalSupply for each token
  const supplies = await Promise.all(
    CALIBERS.map(caliber =>
      publicClient.readContract({
        address: fuji.calibers[caliber].token,
        abi: AmmoTokenAbi,
        functionName: "totalSupply",
      }).catch(() => BigInt(0))
    )
  );
  // Include in response
  { caliber, name, pricePerRound, priceX18, totalSupply: Math.floor(Number(formatUnits(supplies[i], 18))) }
  ```
- Drop `change7d`, `warehouseBacking`, `fullyBacked` fields (mock only)
- Remove `MarketCaliberData` type import from mock-data

**Caliber header** (`caliber-header.tsx`):

- Replace `CaliberDetailData` and `CaliberId` type imports with `CaliberDetailData` from `@/lib/types.ts` and `Caliber` from shared
- This component receives its data as props from the parent page -- just fix the type imports

**Action panel** (`action-panel.tsx`):

- Same as caliber-header: replace type imports with real types from `@/lib/types.ts` and shared

**Token stats** (`token-stats.tsx`):

- Replace `CaliberDetailData` type import with the version from `@/lib/types.ts`
- Component receives data as props -- just fix the import

**Price chart** (`price-chart.tsx`):

- Replace `CaliberId` with `Caliber` from shared
- Replace `chartDataByCaliber` with a placeholder: either show a "Historical data coming soon" message, OR generate simple chart data from the current price as a flat line
- Recommendation: show a single-point or flat line chart at the current price with a "Historical data coming soon" note. This satisfies "no mock data" while being honest about data availability

**Activity feed** (`activity-feed.tsx`):

- Replace `recentActivity` import with a fetch from the new `/api/activity` route
- Map response to display format: type, amount, truncated address, relative time (use date-fns `formatDistanceToNow` or simple relative time helper)
- Show loading skeleton while fetching, empty state if no activity

**Swap widget** (`swap-widget.tsx`):

- Replace `caliberDetails` and `CaliberId` imports
- Fetch prices from `/api/market` or receive as props
- Use `Caliber` from shared instead of `CaliberId`
- Use `CALIBER_SPECS` for static caliber info (name, specLine, minMint, fees)

**Protocol stats** (`protocol-stats.tsx`):

- Replace `protocolStats` import with real data:
  - Total rounds tokenized: fetch `/api/market` which now includes totalSupply, sum across calibers
  - TVL: sum(totalSupply \* pricePerRound) across calibers, format as "$X.XK" or "$X.XM"
  - Unique holders: query DB `prisma.user.count()` via a new field in the response, or just show "--" if no API exists (adding a user count to /api/market or a separate /api/stats is acceptable)
  - 24h volume: show "--" (no real volume tracking exists)
- Use useEffect + fetch pattern for client components, or direct Prisma call for server components

**Caliber detail page** (`app/market/[caliber]/page.tsx`):

- Replace `caliberDetails[caliber]` mock lookup with fetch from `/api/market`
- Filter the response to find the matching caliber
- Construct CaliberDetailData from API response + CALIBER_SPECS for static fields
- Pass constructed data to child components (caliber-header, action-panel, token-stats, price-chart, activity-feed)

**For all files:**

- Remove ALL imports from `@/lib/mock-data`
- Use `Caliber` from `@ammo-exchange/shared` instead of `CaliberId` from mock-data
- Use `CaliberDetailData` from `@/lib/types.ts` where needed
- Use `CALIBER_SPECS` from shared for static caliber properties (name, description, grainWeight, minMintRounds)
  </action>
  <verify>

1. Run `pnpm --filter @ammo-exchange/web check` -- TypeScript passes
2. `grep -r "mock-data" apps/web/features/market/ apps/web/features/home/ apps/web/features/trade/ apps/web/app/market/` returns zero results
3. `curl` or test `/api/activity` returns JSON with activity array
   </verify>
   <done>
   All market, home, trade, and caliber detail components use real data from /api/market, /api/activity, CALIBER_SPECS, and on-chain reads. No mock-data imports in any of these files. /api/market response includes totalSupply. /api/activity route exists and returns recent completed orders.
   </done>
   </task>

<task type="auto">
  <name>Task 2: Remove residual mock-data imports from mint/redeem flows and delete mock-data.ts</name>
  <files>
    apps/web/features/mint/mint-flow.tsx
    apps/web/features/redeem/redeem-flow.tsx
    apps/web/lib/mock-data.ts
  </files>
  <action>
**Mint flow** (`mint-flow.tsx`):
- Check what it still imports from mock-data. Based on the grep, it imports `caliberDetails` and `CaliberDetailData` types.
- These were partially wired in Phase 4 but still reference mock caliberDetails for price display and caliber metadata.
- Replace: fetch caliber price from `/api/market` or receive as prop. Use `CALIBER_SPECS[caliber]` for static fields (name, specLine/description, minMint, fees).
- Replace `CaliberDetailData` type with the one from `@/lib/types.ts`
- Replace `CaliberId` with `Caliber` from shared
- The mint flow already uses real hooks for transactions (Phase 4). This task only removes the remaining mock-data references for display data.

**Redeem flow** (`redeem-flow.tsx`):

- Same pattern as mint-flow: replace `caliberDetails`, `CaliberId`, `CaliberDetailData` imports
- Use `/api/market` for prices and `CALIBER_SPECS` for static metadata
- Replace `CaliberId` with `Caliber` from shared
- The redeem flow already uses real hooks for transactions (Phase 4). This task only removes the remaining mock-data references for display data.

**Delete mock-data.ts** (`apps/web/lib/mock-data.ts`):

- After all imports are removed, delete the entire file
- This is the final step -- do it LAST after confirming no imports remain

**Final audit:**

- Run `grep -r "mock-data" apps/web/` to confirm zero results
- Run `pnpm --filter @ammo-exchange/web check` to confirm no TypeScript errors
- Run `pnpm --filter @ammo-exchange/web build` to confirm Next.js build succeeds

The build must succeed because "no mock data remains" is only true if the build proves no reference to the deleted file.
</action>
<verify>

1. `grep -r "mock-data" apps/web/` returns zero results (no file imports from deleted module)
2. `pnpm --filter @ammo-exchange/web check` passes
3. `pnpm --filter @ammo-exchange/web build` succeeds
4. `ls apps/web/lib/mock-data.ts` returns "No such file" (file deleted)
   </verify>
   <done>
   mock-data.ts is deleted. Zero files in the codebase import from it. Mint and redeem flows use /api/market for prices and CALIBER_SPECS for static metadata. TypeScript check and Next.js build both pass. The entire app runs on real data.
   </done>
   </task>

</tasks>

<verification>
1. `grep -r "mock-data" apps/web/` returns zero results
2. `ls apps/web/lib/mock-data.ts` returns "No such file or directory"
3. `pnpm --filter @ammo-exchange/web check` passes with zero errors
4. `pnpm --filter @ammo-exchange/web build` succeeds
5. `grep "api/activity" apps/web/features/market/activity-feed.tsx` returns at least one result
6. `grep "api/market" apps/web/features/market/market-ticker.tsx` returns at least one result (or equivalent prop)
7. `grep "CALIBER_SPECS" apps/web/features/home/protocol-stats.tsx apps/web/app/market/\\[caliber\\]/page.tsx` returns results
</verification>

<success_criteria>

- mock-data.ts is deleted from the codebase
- Zero files import from @/lib/mock-data
- All 13 remaining files (market-ticker, market-cards, market-table, caliber-header, action-panel, token-stats, price-chart, activity-feed, swap-widget, protocol-stats, mint-flow, redeem-flow, market/[caliber]/page) use real data sources
- /api/activity route exists and returns recent completed orders
- /api/market response includes totalSupply per caliber
- pnpm build succeeds end-to-end
  </success_criteria>

<output>
After completion, create `.planning/phases/05-portfolio-and-data-integration/05-02-SUMMARY.md`
</output>
