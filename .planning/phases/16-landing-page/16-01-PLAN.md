---
phase: 16-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/features/home/hero.tsx
  - apps/web/features/home/faq.tsx
  - apps/web/features/home/index.ts
  - apps/web/features/market/market-cards.tsx
  - apps/web/app/(landing)/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor sees hero with protocol tagline and a prominent Launch App CTA button"
    - "Visitor sees how-it-works section with 3 visual steps (mint, trade, redeem)"
    - "Visitor sees caliber showcase displaying all 4 calibers with specs (grain, type, min order) and current prices"
    - "Visitor sees FAQ section with common protocol questions and expandable answers"
  artifacts:
    - path: "apps/web/features/home/hero.tsx"
      provides: "Hero section with Launch App CTA"
      contains: "Launch App"
    - path: "apps/web/features/home/faq.tsx"
      provides: "FAQ accordion component"
      exports: ["Faq"]
    - path: "apps/web/features/home/index.ts"
      provides: "Barrel export including Faq"
      contains: "Faq"
    - path: "apps/web/features/market/market-cards.tsx"
      provides: "Caliber cards with specs (grain, type, min order)"
      contains: "grainWeight"
    - path: "apps/web/app/(landing)/page.tsx"
      provides: "Landing page with all sections including FAQ"
      contains: "Faq"
  key_links:
    - from: "apps/web/app/(landing)/page.tsx"
      to: "apps/web/features/home/faq.tsx"
      via: "import from @/features/home"
      pattern: "import.*Faq.*from.*features/home"
    - from: "apps/web/features/market/market-cards.tsx"
      to: "@ammo-exchange/shared"
      via: "CALIBER_SPECS import for grain/type data"
      pattern: "CALIBER_SPECS"
---

<objective>
Complete the public landing page with updated hero CTA, caliber specs in showcase, and FAQ section.

Purpose: Phase 16 is the final phase of v1.3. The landing page is the first thing visitors see. It must explain the protocol clearly and drive them to connect a wallet via "Launch App".

Output: Polished landing page with hero (Launch App CTA), how-it-works (already exists), caliber showcase (enhanced with specs), and FAQ section.
</objective>

<execution_context>
@/Users/chiranjibipoudyal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiranjibipoudyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/web/features/home/hero.tsx
@apps/web/features/home/how-it-works.tsx
@apps/web/features/home/protocol-stats.tsx
@apps/web/features/home/index.ts
@apps/web/features/market/market-cards.tsx
@apps/web/app/(landing)/page.tsx
@apps/web/app/(landing)/layout.tsx
@packages/shared/src/constants/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Hero CTA and enhance caliber showcase with specs</name>
  <files>
    apps/web/features/home/hero.tsx
    apps/web/features/market/market-cards.tsx
  </files>
  <action>
**hero.tsx changes:**
1. Change primary CTA text from "Start Minting" to "Launch App"
2. Change primary CTA href from "/mint" to "/dashboard"
3. Keep secondary CTA "View Market" as-is (scrolls to #market section which shows caliber showcase)
4. Keep all existing styling, SVG background, trust strip -- only change CTA text and href

**market-cards.tsx changes (caliber showcase enhancement):**

1. Import `CALIBER_SPECS` from `@ammo-exchange/shared` and `import type { Caliber } from "@ammo-exchange/shared"`
2. In the `CaliberCard` component, after the existing "Supply: X rounds" line, add a specs row showing:
   - Grain weight: `{CALIBER_SPECS[caliber.caliber as Caliber].grainWeight}gr`
   - Case type: `{CALIBER_SPECS[caliber.caliber as Caliber].caseType}` (capitalize first letter)
   - Min order: `{CALIBER_SPECS[caliber.caliber as Caliber].minMintRounds} rd min`
3. Style the specs row as a flex row with gap-3, text-xs, color var(--text-muted) -- same style as existing Supply line
4. Each spec item should be separated by a middot or pipe character for visual separation

Keep all existing hover effects, skeleton loaders, and section structure. The SectionTitle "Live Market" can stay or be updated to "Caliber Showcase" -- use "Live Market" to keep consistency.
</action>
<verify>
Run `pnpm --filter @ammo-exchange/web check` -- no type errors. Grep hero.tsx for "Launch App" and "/dashboard". Grep market-cards.tsx for "CALIBER_SPECS" and "grainWeight".
</verify>
<done>
Hero CTA says "Launch App" linking to /dashboard. Caliber cards show grain weight, case type, and minimum order alongside price and supply.
</done>
</task>

<task type="auto">
  <name>Task 2: Create FAQ section and wire into landing page</name>
  <files>
    apps/web/features/home/faq.tsx
    apps/web/features/home/index.ts
    apps/web/app/(landing)/page.tsx
  </files>
  <action>
**faq.tsx — Create new file:**
1. Create a client component (`"use client"`) with an accordion-style FAQ
2. Use the same `SectionTitle` pattern from how-it-works.tsx (centered uppercase heading with brass underline)
3. Section title: "Frequently Asked Questions"
4. Implement a simple accordion using React useState -- each item expands/collapses on click. No external dependency needed.
5. FAQ items (6-8 questions covering protocol basics):
   - "What is Ammo Exchange?" → Brief explanation: DeFi protocol for tokenized ammunition on Avalanche. Deposit USDC, receive tokens backed 1:1 by physical rounds in insured storage.
   - "How are ammo tokens backed?" → Each token represents 1 round stored in ATF-compliant, insured facilities via Ammo Squared. Tokens are ERC20 on Avalanche.
   - "Who can mint and trade?" → Anyone with USDC and an Avalanche wallet can mint and trade tokens. No KYC required for minting or trading.
   - "Who can redeem for physical ammo?" → Only verified U.S. residents in allowed states. KYC verification required. Must be 18+ for rifle, 21+ for handgun ammo. Some states restricted (CA, NY, IL, DC, NJ).
   - "What are the fees?" → 1.5% on minting and 1.5% on redemption. No fees for trading on DEXs.
   - "What calibers are supported?" → 9mm Luger (115gr FMJ), 5.56 NATO (55gr FMJ), .22 LR (40gr), .308 Winchester (147gr FMJ).
   - "How does the 2-step settlement work?" → You initiate a mint or redeem order. The protocol admin verifies and finalizes. For mints, ammo is purchased and stored. For redeems, ammo is shipped via UPS Ground.
   - "Is this on mainnet?" → Currently live on Avalanche Fuji testnet. Mainnet deployment planned after full validation.
6. Style each FAQ item:
   - Question: text-sm font-medium, color var(--text-primary), cursor-pointer, flex with ChevronDown icon from lucide-react that rotates when open
   - Answer: text-sm leading-relaxed, color var(--text-secondary), with smooth height transition (or simple conditional render)
   - Items separated by border-b with color var(--border-default)
   - Container: max-w-3xl mx-auto
7. Section wrapper: py-24 px-4 lg:py-32, backgroundColor var(--bg-secondary)
8. Export as named export: `export function Faq()`

**index.ts — Update barrel export:**
Add `export { Faq } from "./faq";` to existing exports.

**page.tsx — Wire FAQ into landing page:**

1. Add `Faq` to the import from `@/features/home`
2. Add `<Faq />` between `<ProtocolStats />` and the closing fragment. The order should be:
   - Hero
   - MarketTicker
   - HowItWorks
   - MarketCards (caliber showcase)
   - Faq
   - ProtocolStats (stats bar at bottom, before footer)
     </action>
     <verify>
     Run `pnpm --filter @ammo-exchange/web check` -- no type errors. Verify faq.tsx exists and exports Faq. Verify page.tsx imports and renders Faq. Run `pnpm format` to ensure formatting.
     </verify>
     <done>
     FAQ section renders on landing page with 8 expandable questions covering protocol basics. All sections present in order: Hero, MarketTicker, HowItWorks, MarketCards, Faq, ProtocolStats.
     </done>
     </task>

</tasks>

<verification>
1. `pnpm --filter @ammo-exchange/web check` passes with zero errors
2. `pnpm format:check` passes
3. Landing page at `/` renders all 6 sections in order: Hero, MarketTicker, HowItWorks, MarketCards, Faq, ProtocolStats
4. Hero CTA says "Launch App" and links to /dashboard
5. Caliber cards show grain weight, case type, and min order
6. FAQ has 8 expandable items that toggle open/closed
</verification>

<success_criteria>

- Visitor sees hero with "Launch App" CTA linking to /dashboard
- Visitor sees how-it-works with 3 visual mint/trade/redeem steps
- Visitor sees 4 caliber cards with price, supply, grain weight, case type, and min order
- Visitor sees FAQ with 8 expandable protocol questions
- All sections render on the landing page in logical order
- No type errors, formatting clean
  </success_criteria>

<output>
After completion, create `.planning/phases/16-landing-page/16-01-SUMMARY.md`
</output>
