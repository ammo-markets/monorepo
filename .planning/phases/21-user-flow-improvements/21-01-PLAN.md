---
phase: 21-user-flow-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/features/mint/mint-flow.tsx
  - apps/web/features/trade/trade-tabs.tsx
autonomous: true

must_haves:
  truths:
    - "User sees 24-48 hour processing time disclosure prominently before entering a mint amount"
    - "User sees a disclaimer that admin sets the final price at fulfillment, before confirming"
    - "Swap tab displays a Coming Soon badge with explanation of what it will offer"
  artifacts:
    - path: "apps/web/features/mint/mint-flow.tsx"
      provides: "Processing time banner and price disclaimer in mint flow"
      contains: "24-48 hours"
    - path: "apps/web/features/trade/trade-tabs.tsx"
      provides: "Coming Soon state for swap tab"
      contains: "Coming Soon"
  key_links:
    - from: "apps/web/features/trade/trade-tabs.tsx"
      to: "swap tab content"
      via: "activeTab === swap conditional rendering"
      pattern: "Coming Soon"
---

<objective>
Add prominent mint flow disclosures (processing time, price disclaimer) and replace the swap tab with a Coming Soon placeholder.

Purpose: Users must understand the 24-48h processing time and admin-set pricing before committing USDC. The swap tab should not mislead users into thinking swap functionality exists.
Output: Updated mint-flow.tsx with disclosures, updated trade-tabs.tsx with Coming Soon swap state.
</objective>

<execution_context>
@/Users/chiranjibipoudyal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiranjibipoudyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/features/mint/mint-flow.tsx
@apps/web/features/trade/trade-tabs.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add processing time and price disclaimers to mint flow</name>
  <files>apps/web/features/mint/mint-flow.tsx</files>
  <action>
Add two disclosure elements to the MintFlow component:

1. **Processing time banner on StepEnterAmount (Step 2)** -- Above the USDC input section, add an info banner:
   - Use a rounded-lg container with `backgroundColor: "rgba(243, 156, 18, 0.08)"` and `borderLeft: "3px solid var(--amber)"` (same pattern as existing disclaimer boxes in the codebase).
   - Include a Clock icon (already imported) and text: "Minting takes 24-48 hours. Your USDC is held in the smart contract until an admin verifies and finalizes your order."
   - Style: text-xs, leading-relaxed, color: var(--text-secondary)

2. **Price disclaimer on StepReview (Step 3)** -- Below the existing amber disclaimer box (the one that says "Tokens will be minted after physical ammunition is verified..."), add a second disclaimer box:
   - Use the same styling pattern but with `borderLeft: "3px solid var(--text-muted)"` to differentiate
   - Include an Info icon and text: "The final token amount is determined by the admin at fulfillment based on the current market price. The estimate shown above may differ from the actual tokens received."
   - Style: text-xs, leading-relaxed, color: var(--text-secondary)

Do NOT remove any existing disclaimer boxes. These are additive.
  </action>
  <verify>Run `pnpm --filter @ammo-exchange/web check` to confirm no type errors. Visually confirm the two new disclosure elements render in the component file.</verify>
  <done>StepEnterAmount shows a processing time banner above the input. StepReview shows a price disclaimer below the existing disclaimer. Both use consistent styling patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Replace swap tab with Coming Soon placeholder</name>
  <files>apps/web/features/trade/trade-tabs.tsx</files>
  <action>
Replace the swap tab content in TradeTabs component. Currently when `activeTab === "swap"`, it renders `<SwapWidget defaultOpen />`. Replace that with a Coming Soon placeholder:

1. In the TABS constant array, update the swap tab label from "Swap" to include a badge. Change the tab button rendering so when `tab.id === "swap"`, the label shows "Swap" followed by a small "Coming Soon" badge span:
   - Badge: `className="ml-1.5 rounded-full px-1.5 py-0.5 text-[10px] font-semibold"` with `style={{ backgroundColor: "rgba(243, 156, 18, 0.15)", color: "var(--amber)" }}`
   - Text: "Soon"

2. Replace the swap tab content (the `activeTab === "swap"` block) with a centered placeholder:
   ```
   <div className="flex flex-col items-center justify-center py-16 text-center">
     <div className="mb-4 flex h-14 w-14 items-center justify-center rounded-full"
       style={{ backgroundColor: "rgba(243, 156, 18, 0.1)", border: "1.5px solid var(--amber)" }}>
       <ArrowDownUp size={24} style={{ color: "var(--amber)" }} />
     </div>
     <h3 className="mb-2 text-lg font-semibold" style={{ color: "var(--text-primary)" }}>
       Swap Coming Soon
     </h3>
     <p className="max-w-sm text-sm leading-relaxed" style={{ color: "var(--text-secondary)" }}>
       Trade ammo tokens directly with other users via decentralized exchange pools.
       Swap between calibers or convert to USDC without the 24-48h settlement window.
     </p>
     <p className="mt-4 text-xs" style={{ color: "var(--text-muted)" }}>
       Powered by Uniswap V3 on Avalanche
     </p>
   </div>
   ```

3. Remove the `SwapWidget` import from trade-tabs.tsx since it's no longer used there. Keep the SwapWidget component file itself untouched (it may be used elsewhere or in the future).

4. The ArrowDownUp icon is already imported in trade-tabs.tsx from lucide-react.
  </action>
  <verify>Run `pnpm --filter @ammo-exchange/web check` to confirm no type errors. Verify the SwapWidget import is removed and the Coming Soon placeholder renders.</verify>
  <done>Swap tab shows "Swap" with a "Soon" badge. Clicking the swap tab shows a Coming Soon placeholder with explanation about what swap will offer (DEX trading, caliber conversion, no settlement window). SwapWidget import removed from trade-tabs.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @ammo-exchange/web check` passes with no errors
2. MintFlow Step 2 (Enter Amount) has processing time disclosure banner
3. MintFlow Step 3 (Review) has price disclaimer below existing disclaimer
4. Swap tab button shows "Soon" badge
5. Swap tab content shows Coming Soon placeholder instead of SwapWidget
</verification>

<success_criteria>
- Processing time disclosure visible before user enters amount (MINT-01)
- Price disclaimer visible before user confirms mint (MINT-02)
- Swap tab clearly labeled Coming Soon with explanation (TRAD-01)
- No type errors in web package
</success_criteria>

<output>
After completion, create `.planning/phases/21-user-flow-improvements/21-01-SUMMARY.md`
</output>
