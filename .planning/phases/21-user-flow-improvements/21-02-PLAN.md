---
phase: 21-user-flow-improvements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/features/redeem/redeem-flow.tsx
  - apps/web/app/(app)/profile/page.tsx
autonomous: true

must_haves:
  truths:
    - "Starting a redeem flow checks KYC status first and blocks unverified users with a prompt"
    - "User can initiate and complete KYC verification directly from the Profile page"
  artifacts:
    - path: "apps/web/features/redeem/redeem-flow.tsx"
      provides: "KYC pre-check gate before redeem Step 0"
      contains: "kycStatus"
    - path: "apps/web/app/(app)/profile/page.tsx"
      provides: "Embedded KYC form on profile page for unverified users"
      contains: "KycForm"
  key_links:
    - from: "apps/web/app/(app)/profile/page.tsx"
      to: "apps/web/features/redeem/kyc-form.tsx"
      via: "KycForm import and rendering"
      pattern: "import.*KycForm"
    - from: "apps/web/app/(app)/profile/page.tsx"
      to: "apps/web/hooks/use-kyc.ts"
      via: "useKycStatus and useKycSubmit hooks"
      pattern: "useKycStatus|useKycSubmit"
---

<objective>
Add KYC pre-check to the redeem flow (blocks unverified users before they start) and embed KYC verification directly on the profile page.

Purpose: Users should not waste time filling out shipping info only to discover KYC is required. Profile page should be a standalone path to complete verification.
Output: Updated redeem-flow.tsx with KYC gate, updated profile/page.tsx with embedded KYC form.
</objective>

<execution_context>
@/Users/chiranjibipoudyal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiranjibipoudyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/features/redeem/redeem-flow.tsx
@apps/web/app/(app)/profile/page.tsx
@apps/web/features/redeem/kyc-form.tsx
@apps/web/hooks/use-kyc.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add KYC pre-check gate to redeem flow</name>
  <files>apps/web/features/redeem/redeem-flow.tsx</files>
  <action>
Add a KYC status check that intercepts BEFORE the redeem flow begins (before Step 0). The current flow is:
- Step 0: Select caliber & amount
- Step 1: Shipping
- Step 2: KYC
- Step 3: Review
- Step 4: Confirmation

Change: When the user clicks "Next" on Step 0 to proceed, check `kycStatus`. The hooks `useKycStatus` and `useKycSubmit` are already imported and used in the RedeemFlow component (kycStatus, kycData, kycPrefill, etc. are already wired).

Implementation approach -- add a KYC interstitial step:

1. In the RedeemFlow orchestrator, BEFORE rendering Step 0, check if the wallet is connected AND kycStatus is loaded AND kycStatus is NOT "APPROVED". If so, show a KYC prompt overlay/banner at the TOP of Step 0 (not a separate step -- this avoids renumbering all steps).

2. Add a `showKycPrompt` state, initialized to false. After market data and KYC data load, if kycStatus is "NONE" or "REJECTED", set `showKycPrompt = true`.

3. When `showKycPrompt` is true AND wallet is connected, render a prominent banner ABOVE the Step 0 content:
   - Use a rounded-xl container with `backgroundColor: "rgba(243, 156, 18, 0.08)"` and `border: "1.5px solid var(--amber)"`
   - Shield icon (already imported) + heading "Identity Verification Required"
   - Text: "You must complete KYC verification before redeeming tokens for physical delivery. This is a one-time process required by federal law."
   - Two buttons:
     a. Primary (brass): "Complete Verification" -- links to `/profile#kyc` (where we'll add the form in Task 2)
     b. Secondary (ghost): "Dismiss" -- sets `showKycPrompt = false` to let user browse but NOT proceed

4. Modify the "Next" button handler in StepSelectCaliberAmount: when the user clicks Next on Step 0, if kycStatus is NOT "APPROVED" and NOT "PENDING", show the KYC prompt again (set `showKycPrompt = true`) and do NOT advance to Step 1. If kycStatus is "PENDING", allow proceeding (they'll hit the pending state at Step 2 as before). If "APPROVED", proceed normally.

This approach:
- Shows the warning upfront (before user invests time)
- Still allows browsing caliber prices
- Blocks progression until KYC is started
- Does NOT renumber existing steps (KYC at Step 2 still works for the pending check)
  </action>
  <verify>Run `pnpm --filter @ammo-exchange/web check` to confirm no type errors. Verify the KYC banner renders in the component when kycStatus is NONE.</verify>
  <done>Unverified users see a KYC prompt when starting a redeem flow. They cannot proceed past Step 0 without starting KYC. The prompt links to the profile page for verification.</done>
</task>

<task type="auto">
  <name>Task 2: Add KYC verification form to profile page</name>
  <files>apps/web/app/(app)/profile/page.tsx</files>
  <action>
Embed the KYC form directly on the profile page so users can complete verification without going through the redeem flow.

1. **Import KycForm and hooks**: Add imports:
   - `import { KycForm } from "@/features/redeem/kyc-form";`
   - `import type { KycFormData } from "@/features/redeem/kyc-form";`
   - `import { useKycStatus, useKycSubmit } from "@/hooks/use-kyc";`
   - `import { useAuth } from "@/contexts/auth-context";` (already imported)

2. **Wire KYC hooks**: In ProfilePage component, add:
   - `const { address } = useAuth();` -- need wallet address for KYC hook (check if auth context exposes address; if not, use profile.walletAddress from existing query)
   - `const { data: kycData, isLoading: kycLoading } = useKycStatus(profile?.walletAddress);`
   - `const { mutateAsync: submitKyc, isPending: kycSubmitting } = useKycSubmit();`
   - Derive `kycStatus` from kycData (fallback to profile.kycStatus if kycData not loaded)
   - Derive `kycPrefill` from kycData

3. **Replace Section B (KYC Status)**: Currently Section B only shows a badge and a "Complete verification" link to /trade?tab=redeem. Replace with conditional rendering:

   **If kycStatus === "APPROVED"**: Show current badge (Verified) -- no changes needed.

   **If kycStatus === "PENDING"**: Show badge (Pending) + informational text: "Your identity verification is being reviewed. This usually takes a few minutes to a few hours."

   **If kycStatus === "NONE" or "REJECTED"**: Show badge + embedded KycForm component:
   - Heading stays the same (Identity Verification section)
   - Below the badge, show the KycForm:
     ```
     <div className="mt-4" id="kyc">
       <p className="mb-4 text-sm leading-relaxed" style={{ color: "var(--text-secondary)" }}>
         Complete identity verification to enable physical ammunition redemption. This is a one-time process.
       </p>
       <KycForm
         onSubmit={handleKycSubmit}
         isSubmitting={kycSubmitting}
         prefill={kycPrefill}
       />
     </div>
     ```
   - Add a `handleKycSubmit` callback: `async (data: KycFormData) => { await submitKyc(data); }`
   - Remove the "Complete verification" link to /trade?tab=redeem (replaced by inline form)

4. **Add id="kyc" anchor**: Add `id="kyc"` to the KYC section's container div so the redeem flow can link to `/profile#kyc` directly.

5. **Invalidate profile query on KYC submit**: After KYC submission succeeds, the useKycSubmit hook already invalidates the ["kyc"] query. Also invalidate ["profile"] to refresh the badge:
   - In the handleKycSubmit, after `await submitKyc(data)`, call `queryClient.invalidateQueries({ queryKey: ["profile"] })`.
   - Import `useQueryClient` from @tanstack/react-query (may already be used via useQuery).

Note: The KycForm component is exported from `@/features/redeem/kyc-form` and its KycFormData type is also exported. It accepts `onSubmit`, `isSubmitting`, and optional `prefill` props.
  </action>
  <verify>Run `pnpm --filter @ammo-exchange/web check` to confirm no type errors. Verify the KycForm renders on the profile page when kycStatus is NONE.</verify>
  <done>Profile page shows embedded KYC form for unverified users with id="kyc" anchor. KYC submission works directly from profile. Badge refreshes after submission. Verified users see only the green Verified badge.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @ammo-exchange/web check` passes with no errors
2. Redeem flow shows KYC prompt banner for unverified users before Step 0
3. Unverified users cannot proceed past Step 0 in redeem flow
4. Profile page shows embedded KYC form for unverified users
5. Profile page shows Pending status for users with pending KYC
6. Profile page shows Verified badge for approved users (no form)
7. KYC form submission from profile page works (invalidates queries)
</verification>

<success_criteria>
- KYC pre-check blocks unverified users at redeem start (REDM-01, REDM-02)
- Profile page has standalone KYC completion (PROF-01)
- No type errors in web package
</success_criteria>

<output>
After completion, create `.planning/phases/21-user-flow-improvements/21-02-SUMMARY.md`
</output>
