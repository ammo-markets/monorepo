---
phase: 28-data-flow-completion
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - apps/web/features/admin/mint-orders-table.tsx
  - apps/web/features/admin/redeem-orders-table.tsx
  - apps/web/features/admin/finalize-mint-dialog.tsx
  - apps/web/features/admin/finalize-redeem-dialog.tsx
  - apps/web/features/admin/order-detail-drawer.tsx
  - apps/web/features/portfolio/orders-row.tsx
  - apps/web/features/portfolio/order-detail.tsx
  - apps/web/features/dashboard/recent-orders.tsx
  - apps/web/features/market/activity-feed.tsx
  - apps/web/features/redeem/redeem-flow.tsx
autonomous: true

must_haves:
  truths:
    - "Mint order displays show USDC amount from usdcAmount field (not tokenAmount)"
    - "Redeem order displays show token rounds from tokenAmount field (not usdcAmount)"
    - "Admin mint orders table shows USDC amount from order.usdcAmount"
    - "Admin redeem orders table shows token amount from order.tokenAmount"
    - "Activity feed renders amount correctly for both mint and redeem entries"
    - "Redeem flow persists shipping address to user profile via PATCH /api/users/profile before advancing past step 1"
    - "Portfolio and dashboard order rows show context-appropriate amounts"
  artifacts:
    - path: "apps/web/features/admin/mint-orders-table.tsx"
      provides: "Mint table with usdcAmount display"
      contains: "order.usdcAmount"
    - path: "apps/web/features/admin/redeem-orders-table.tsx"
      provides: "Redeem table with tokenAmount display"
      contains: "order.tokenAmount"
    - path: "apps/web/features/redeem/redeem-flow.tsx"
      provides: "Shipping persistence before confirmation"
      contains: "api/users/profile"
  key_links:
    - from: "apps/web/features/admin/mint-orders-table.tsx"
      to: "order.usdcAmount"
      via: "formatUsdc renders USDC cost"
      pattern: "formatUsdc.*usdcAmount"
    - from: "apps/web/features/admin/redeem-orders-table.tsx"
      to: "order.tokenAmount"
      via: "formatTokenAmount renders token rounds"
      pattern: "formatTokenAmount.*tokenAmount"
    - from: "apps/web/features/redeem/redeem-flow.tsx"
      to: "/api/users/profile"
      via: "PATCH call to persist shipping before onNext"
      pattern: "api/users/profile"
---

<objective>
Update all UI components to display amounts from the correct normalized field (usdcAmount for mint, tokenAmount for redeem) and wire the redeem flow's shipping step to persist the address to the database before advancing.

Purpose: Completes the data flow from normalized DB fields through APIs to user-facing displays. Satisfies DATA-04 (correct amount rendering), DATA-06 (shipping persistence), and ensures no component references the deleted `amount` field.
Output: All UI renders correct amounts; shipping address persisted on redeem flow step 1.
</objective>

<execution_context>
@/Users/chiranjibipoudyal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiranjibipoudyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-data-flow-completion/28-01-SUMMARY.md

Key context from Plan 01:

- OrderFromAPI now has `usdcAmount: string | null` and `tokenAmount: string | null` (no `amount`)
- MarketCaliberFromAPI.totalSupply is now `string` (was `number`)
- CaliberDetailData.totalSupply is now `string` (was `number`)
- AdminMintOrder and AdminRedeemOrder interfaces (in finalize dialogs) still have `amount: string` -- must update
- Activity feed already reads `updatedAt` from API response (hook has it)

Current amount display patterns to migrate:

- Admin mint: `formatUsdc(order.amount)` -> `formatUsdc(order.usdcAmount ?? "0")`
- Admin redeem: `formatTokenAmount(order.amount)` -> `formatTokenAmount(order.tokenAmount ?? "0")`
- Portfolio/dashboard: `Math.floor(Number(order.amount))` -> context-dependent (mint: usdcAmount, redeem: tokenAmount)
- Activity feed: `Number(item.amount).toLocaleString()` -> already correct field from API

Shipping flow:

- Redeem step 1 (StepShipping) currently calls `onNext()` without saving anything
- The shipping API (POST /api/redeem/shipping) requires orderId which doesn't exist at step 1
- Solution: Persist shipping to user's default profile via PATCH /api/users/profile at step 1
- User profile PATCH endpoint already exists and accepts defaultShipping\* fields
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Update admin components to use usdcAmount/tokenAmount</name>
  <files>
    apps/web/features/admin/mint-orders-table.tsx
    apps/web/features/admin/redeem-orders-table.tsx
    apps/web/features/admin/finalize-mint-dialog.tsx
    apps/web/features/admin/finalize-redeem-dialog.tsx
    apps/web/features/admin/order-detail-drawer.tsx
  </files>
  <action>
**AdminMintOrder interface (`finalize-mint-dialog.tsx`):**
1. Replace `amount: string;` with `usdcAmount: string | null; tokenAmount: string | null;`

**AdminRedeemOrder interface (`finalize-redeem-dialog.tsx`):**

1. Replace `amount: string;` with `usdcAmount: string | null; tokenAmount: string | null;`

**Mint orders table (`mint-orders-table.tsx`):**

1. Replace `{formatUsdc(order.amount)} USDC` with `{formatUsdc(order.usdcAmount ?? "0")} USDC`
2. Any other `order.amount` references -> `order.usdcAmount ?? "0"`

**Finalize mint dialog (`finalize-mint-dialog.tsx`):**

1. Replace `{formatUsdc(order.amount)} USDC` with `{formatUsdc(order.usdcAmount ?? "0")} USDC`

**Redeem orders table (`redeem-orders-table.tsx`):**

1. Replace `{formatTokenAmount(order.amount)} rounds` with `{formatTokenAmount(order.tokenAmount ?? "0")} rounds`

**Finalize redeem dialog (`finalize-redeem-dialog.tsx`):**

1. Replace `{formatTokenAmount(order.amount)} rounds` with `{formatTokenAmount(order.tokenAmount ?? "0")} rounds`

**Order detail drawer (`order-detail-drawer.tsx`):**

1. Replace the conditional amount display:
   - Old: `order.type === "MINT" ? formatUsdc(order.amount) USDC : formatTokenAmount(order.amount) rounds`
   - New: `order.type === "MINT" ? formatUsdc(order.usdcAmount ?? "0") USDC : formatTokenAmount(order.tokenAmount ?? "0") rounds`
2. The drawer uses a union type for order -- update it to include `usdcAmount`/`tokenAmount` instead of `amount`.
   </action>
   <verify>
   Run `pnpm --filter @ammo-exchange/web check` -- no type errors on admin component files. Grep `apps/web/features/admin/` for `order.amount` to confirm zero occurrences.
   </verify>
   <done>
   All admin components display usdcAmount for mint orders and tokenAmount for redeem orders. No references to deleted `amount` field in admin features. AdminMintOrder and AdminRedeemOrder interfaces updated.
   </done>
   </task>

<task type="auto">
  <name>Task 2: Update portfolio, dashboard, activity components and wire redeem shipping persistence</name>
  <files>
    apps/web/features/portfolio/orders-row.tsx
    apps/web/features/portfolio/order-detail.tsx
    apps/web/features/dashboard/recent-orders.tsx
    apps/web/features/market/activity-feed.tsx
    apps/web/features/redeem/redeem-flow.tsx
  </files>
  <action>
**Portfolio orders-row (`orders-row.tsx`):**
1. This component renders both mint and redeem orders. Replace `Math.floor(Number(order.amount))` with context-appropriate logic:
   - For MINT orders: `order.usdcAmount ? (Number(order.usdcAmount) / 1e6).toFixed(2) + " USDC"`
   - For REDEEM orders: `order.tokenAmount ? Math.floor(Number(order.tokenAmount) / 1e18).toLocaleString() + " rounds"`
   - If the field is null, show "—" as fallback
2. There appear to be two render functions (compact and expanded row). Update both.

**Portfolio order-detail (`order-detail.tsx`):**

1. Replace `Math.floor(Number(order.amount))` with the same context-appropriate display as above.

**Dashboard recent-orders (`recent-orders.tsx`):**

1. Replace `Math.floor(Number(order.amount))` with context-appropriate display matching portfolio pattern.
2. There are two render paths (MintOrderRow and RedeemOrderRow based on grep context). Mint rows show USDC, redeem rows show token rounds.

**Activity feed (`activity-feed.tsx`):**

1. Replace `{Number(item.amount).toLocaleString("en-US")} rounds` with proper display. ActivityLog.amount is the coalesced value (usdcAmount ?? tokenAmount ?? "0" from worker). For activity feed, the display should be contextual:
   - MINT type: Show as USDC amount (divide by 1e6, format as USD)
   - REDEEM type: Show as rounds (divide by 1e18, format as integer)
2. Alternatively, since the activity feed is a simple public feed, showing rounds for all is acceptable if the API provides token amounts. Check what the worker writes to ActivityLog.amount -- per Phase 27, it uses `usdcAmount ?? tokenAmount ?? "0"`. So for MINT it's USDC-wei, for REDEEM it's token-wei. Display must be type-aware.

**Redeem flow shipping persistence (`redeem-flow.tsx`):**

1. In StepShipping's "Next" button handler (around line 710-726), before calling `onNext()`:
   - Make a PATCH request to `/api/users/profile` with the shipping address mapped to defaultShipping fields:
     ```
     defaultShippingName: address.fullName
     defaultShippingLine1: address.address1
     defaultShippingLine2: address.address2 || null
     defaultShippingCity: address.city
     defaultShippingState: address.state
     defaultShippingZip: address.zip
     ```
   - Wrap in try/catch. On success, call `onNext()`. On failure, set saveError and don't advance.
   - This persists the address to the database before the confirmation step.
2. The `isSaving` and `setSaveError` state already exists in the component.
3. Import nothing new -- `fetch` is available globally.

**totalSupply string migration in UI:**
Where CaliberDetailData.totalSupply is used for display, it's now a string. Check if any component does arithmetic on totalSupply. If so, use `Number(totalSupply)` or `parseInt(totalSupply)` for display only (these are supply counts, not BigInt-scale values -- supply of 1000 rounds is safe as Number). The key is the API layer transmits as string; UI can convert for display.
</action>
<verify>

1. `pnpm --filter @ammo-exchange/web check` passes with zero errors
2. `grep -r "order\.amount\b" apps/web/features/` returns zero hits (only usdcAmount/tokenAmount)
3. `grep -r "item\.amount" apps/web/features/market/activity-feed.tsx` returns zero hits
4. `grep "api/users/profile" apps/web/features/redeem/redeem-flow.tsx` confirms shipping persistence call exists
   </verify>
   <done>
   Portfolio, dashboard, and activity components display context-appropriate amounts (USDC for mint, rounds for redeem). Redeem flow persists shipping address to user profile database before advancing past step 1. Zero references to deleted `amount` field remain in any UI component.
   </done>
   </task>

</tasks>

<verification>
1. `pnpm --filter @ammo-exchange/web check` passes with zero errors
2. `grep -rn "\.amount\b" apps/web/features/ apps/web/app/api/` returns zero hits for `order.amount` or `item.amount` (may match other unrelated uses like `roundsAmount` -- those are fine)
3. `grep "api/users/profile" apps/web/features/redeem/redeem-flow.tsx` confirms PATCH call
4. Admin mint table shows `usdcAmount`, admin redeem table shows `tokenAmount`
5. Activity feed displays type-appropriate amounts
</verification>

<success_criteria>
All UI components render the correct amount for context: USDC cost for mint displays, token rounds for redeem displays. No component references the deleted `amount` field. Redeem flow persists shipping address to the database before the user sees the confirmation step. The full data flow from normalized DB fields through APIs to UI is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/28-data-flow-completion/28-02-SUMMARY.md`
</output>
